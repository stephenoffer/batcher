# Ray Data 2.58 `ray.data.Dataset` methods and their Batcher spelling.

[Dataset]
add_column = { status = "mismatch", batcher = "Dataset.map_batches", note = "Ray add_column(col, fn) hands fn a pandas batch (batch_format='pandas' default) and appends its result as `col`. Batcher: map_batches(fn, batch_format='pandas') returning the batch with the column added, or with_columns when the column is an expression.", wave = "WF" }
aggregate = { status = "mismatch", batcher = "Dataset.agg", note = "Ray aggregate(*AggregateFn) is eager and returns a scalar or a dict keyed 'sum(x)'. Batcher agg is lazy and returns a one-row Dataset with alias-named columns. Port as: .agg(...).to_pylist()[0] with aliases 'sum(x)'.", wave = "W0" }
columns = { status = "mismatch", batcher = "Dataset.columns", note = "Ray columns() is a method (fetch_if_missing=True may execute); Batcher columns is a property. Drop the call parentheses.", wave = "W0" }
context = { status = "mismatch", batcher = "bt.active_config", note = "Ray returns the DataContext captured by this Dataset; Batcher configuration is process/context scoped (active_config, config_context), not carried per Dataset.", wave = "W9" }
copy = { status = "canonical", batcher = "Dataset" }
count = { status = "canonical", batcher = "Dataset.count" }
deserialize_lineage = { status = "out_of_scope", reason = "lineage serialization of object-store block refs for Ray fault tolerance; a Batcher plan is rebuilt from its JSON IR and sources" }
drop_columns = { status = "canonical", batcher = "Dataset.drop", template = 'drop_columns(cols) -> self.drop(sem.column_names(cols))' }
explain = { status = "mismatch", batcher = "Dataset.explain", note = "Ray explain() prints the logical and physical plans and returns None; Batcher returns the rendered plan string. Port as: print(ds.explain()).", wave = "W0" }
filter = { status = "param", batcher = "Dataset.filter", need = "callable row predicate fn (today only in ds.ml.filter), expr= as a SQL string (today Dataset.query), fn_args/fn_kwargs/fn_constructor_args/fn_constructor_kwargs, num_gpus, num_cpus, memory, compute (ActorPoolStrategy/TaskPoolStrategy), 3-tuple concurrency (min, max, initial), ray_remote_args, ray_remote_args_fn", note = "Dataset.filter(expr=Expr) already matches; the callable and SQL-string forms need the consolidation.", wave = "WF", template = 'filter(fn=None, expr=None) -> self.filter(sem.expression(expr), sem.expect(fn, None))' }
flat_map = { status = "param", batcher = "Dataset.flat_map", need = "fn_args/fn_kwargs/fn_constructor_args/fn_constructor_kwargs, num_gpus, num_cpus, memory, compute (ActorPoolStrategy/TaskPoolStrategy), 3-tuple concurrency (min, max, initial), ray_remote_args, ray_remote_args_fn", wave = "WF" }
get_dataset_id = { status = "out_of_scope", reason = "Ray execution-internal dataset id used to tag metrics and logs; Batcher has no per-Dataset registration" }
get_internal_block_refs = { status = "out_of_scope", reason = "Ray object-store block references; Batcher moves Arrow batches over Arrow Flight and exposes no object refs" }
get_stats_summary = { status = "mismatch", batcher = "Dataset.stats", note = "Ray returns a DatasetStatsSummary of a previous execution; Batcher stats() executes the query and returns measured per-operator RunStats.", wave = "W0" }
groupby = { status = "alias", batcher = "Dataset.group_by", template = 'groupby(key) -> self.group_by(sem.column_names(key))' }
has_serializable_lineage = { status = "out_of_scope", reason = "lineage serialization of object-store block refs for Ray fault tolerance; a Batcher plan is rebuilt from its JSON IR and sources" }
input_files = { status = "gap", need = "list the source files a Dataset reads", wave = "W8" }
iter_batches = { status = "mismatch", batcher = ["Dataset.iter_batches", "Dataset.ml.to_numpy_batches"], note = "Ray iter_batches defaults to batch_format='default' ({col: ndarray}, nulls become NaN) and batch_size=256; Batcher iter_batches yields pyarrow RecordBatches of engine size and has no batch_format. Port as: ds.ml.to_numpy_batches(batch_size=256). Missing on iter_batches: batch_format=, drop_last=, local_shuffle_buffer_size=/local_shuffle_seed=, prefetch_batches=.", wave = "W2" }
iter_internal_ref_bundles = { status = "out_of_scope", reason = "Ray object-store block references; Batcher moves Arrow batches over Arrow Flight and exposes no object refs" }
iter_jax_batches = { status = "gap", need = "streaming iterator of JAX array batches (Batcher has only eager Dataset.to_jax)", wave = "W8" }
iter_rows = { status = "mismatch", batcher = "Dataset.iter_rows", note = "Ray yields dict rows; Batcher yields tuples by default. Codemod: iter_rows(named=True).", wave = "W0", template = 'iter_rows() -> self.iter_rows(named=True)' }
iter_tf_batches = { status = "mismatch", batcher = "Dataset.ml.to_tf", note = "Ray returns an iterator of TensorFlow tensor batches (batch_size=256); Batcher ds.ml.to_tf returns a tf.data.Dataset of {column: tensor} batches. Pass batch_size=256.", wave = "W8" }
iter_torch_batches = { status = "mismatch", batcher = "Dataset.ml.iter_torch_batches", note = "Ray defaults batch_size=256 and prefetch_batches=1; Batcher defaults batch_size=None (engine morsel size) and prefetch_batches=2, and lives on ds.ml. Pass batch_size=256, prefetch_batches=1.", wave = "W0" }
iterator = { status = "gap", need = "DataIterator object returned from a Dataset (shared iterator with state_dict, iter_* methods)", wave = "W8" }
join = { status = "param", batcher = "Dataset.join", need = "left_suffix=, right_semi/right_anti join types, Ray join_type spellings (left_outer/right_outer/full_outer/left_semi/left_anti map to how=left/right/full/semi/anti), num_partitions/partition_size_hint/aggregator_ray_remote_args", note = "Ray raises when non-key columns overlap without suffixes; Batcher applies suffix='_right'. Batcher puts the key columns first; Ray keeps left column order then right.", wave = "W2" }
limit = { status = "canonical", batcher = "Dataset.limit" }
map = { status = "param", batcher = "Dataset.map", need = "fn_args/fn_kwargs/fn_constructor_args/fn_constructor_kwargs (callable-class fn), num_gpus, num_cpus, memory, compute (ActorPoolStrategy/TaskPoolStrategy), 3-tuple concurrency (min, max, initial), ray_remote_args, ray_remote_args_fn", wave = "WF" }
map_batches = { status = "mismatch", batcher = "Dataset.map_batches", note = "Ray defaults batch_format='default' ({col: ndarray}) and batch_size=None/'auto'; Batcher defaults batch_format='pyarrow'. Codemod passes batch_format='numpy'. Also missing: num_cpus, memory, compute (ActorPoolStrategy/TaskPoolStrategy), 3-tuple concurrency (min, max, initial), ray_remote_args, ray_remote_args_fn, zero_copy_batch, udf_modifying_row_count.", wave = "WF", template = 'map_batches(fn, *, batch_size=None, batch_format="default", fn_args=None, fn_kwargs=None, fn_constructor_args=None, fn_constructor_kwargs=None, num_gpus=None, concurrency=None) -> self.map_batches(fn, batch_size=batch_size, batch_format=sem.ray_batch_format(batch_format), fn_args=fn_args, fn_kwargs=fn_kwargs, fn_constructor_args=fn_constructor_args, fn_constructor_kwargs=fn_constructor_kwargs, num_gpus=num_gpus, concurrency=concurrency)' }
map_batches_internal = { status = "out_of_scope", reason = "Ray-internal map_batches without public validation, used by Ray libraries; not user API" }
materialize = { status = "mismatch", batcher = "Dataset.cache", note = "Ray materialize() executes now and pins blocks in the object store, returning a MaterializedDataset; Batcher cache() is lazy and stores the Arrow result on the first terminal op. Port as: ds = ds.cache(); ds.count() to force it.", wave = "W0" }
max = { status = "param", batcher = "Dataset.max", need = "on=list or None returning a dict keyed 'max(x)', ignore_nulls=False", wave = "W2" }
mean = { status = "param", batcher = "Dataset.mean", need = "on=list or None returning a dict keyed 'mean(x)', ignore_nulls=False", wave = "W2" }
min = { status = "param", batcher = "Dataset.min", need = "on=list or None returning a dict keyed 'min(x)', ignore_nulls=False", wave = "W2" }
mix = { status = "gap", need = "weighted interleave of datasets with MixStoppingCondition", wave = "W8" }
name = { status = "out_of_scope", reason = "user-facing Dataset display name for Ray dashboard/metrics; Batcher Datasets are unnamed plan handles" }
num_blocks = { status = "out_of_scope", reason = "no block concept; Batcher schedules morsels (16,384 rows or 1 MiB) and moves data over Arrow Flight, not the object store" }
random_sample = { status = "mismatch", batcher = "Dataset.sample", note = "Ray keeps each row independently with probability `fraction`; Batcher sample(fraction) keeps rows by a seeded hash of their values, so duplicate rows are kept or dropped together, and seed=None bakes one seed at plan build.", wave = "W0" }
random_shuffle = { status = "mismatch", batcher = "Dataset.shuffle", note = "Ray random_shuffle() without seed draws a new permutation per execution; Batcher shuffle(seed=0) is deterministic, the same permutation every run. Pass a fresh seed per epoch.", wave = "W0" }
randomize_block_order = { status = "mismatch", batcher = "Dataset.shuffle", note = "Batcher has no blocks; the closest is a full row shuffle, which costs a sort on a random key rather than a block reorder and changes row order within blocks too.", wave = "W0" }
rename_columns = { status = "param", batcher = "Dataset.rename", need = "positional list of new names for every column (dict form already matches)", wave = "W2", template = 'rename_columns(names) -> self.rename(sem.mapping(names))' }
repartition = { status = "param", batcher = "Dataset.repartition", need = "target_num_rows_per_block=, shuffle=, keys= (hash partitioning), sort=, strict=; Batcher repartition only lays out the next write's files (num_files/by/target_size_mb)", wave = "W2" }
schema = { status = "mismatch", batcher = "Dataset.schema", note = "Ray schema() is a method returning ray.data.Schema; Batcher schema is a property returning pyarrow.Schema (both expose .names/.types).", wave = "W0" }
select_columns = { status = "canonical", batcher = "Dataset.select", template = 'select_columns(cols) -> self.select(sem.column_names(cols))' }
serialize_lineage = { status = "out_of_scope", reason = "lineage serialization of object-store block refs for Ray fault tolerance; a Batcher plan is rebuilt from its JSON IR and sources" }
set_name = { status = "out_of_scope", reason = "user-facing Dataset display name for Ray dashboard/metrics; Batcher Datasets are unnamed plan handles" }
show = { status = "mismatch", batcher = "Dataset.show", note = "Ray show(limit=20) prints one dict per row; Batcher show(limit=10) prints a table. Pass limit=20.", wave = "W0" }
size_bytes = { status = "mismatch", batcher = "Dataset.memory_usage", note = "Ray size_bytes() returns the total in-memory bytes from block metadata; Batcher memory_usage() returns a per-column dict of estimated bytes. Port as: sum(ds.memory_usage().values()).", wave = "W0" }
sort = { status = "param", batcher = "Dataset.sort", need = "boundaries= (explicit range-partition boundaries)", wave = "W2", template = 'sort(key, descending=False, boundaries=None) -> self.sort(sem.column_names(key), sem.expect(boundaries, None), descending=descending)' }
split = { status = "mismatch", batcher = "Dataset.split_proportionately", note = "Ray split(n) eagerly materializes n contiguous MaterializedDatasets, and equal=True drops the remainder; Batcher parts are lazy and each re-executes the input. split_proportionately([1/n]*(n-1)) gives the contiguous n-way split; equal= and locality_hints have no equivalent.", wave = "W8" }
split_at_indices = { status = "mismatch", batcher = "Dataset.split_at_indices", note = "Ray materializes once and returns MaterializedDatasets; Batcher parts are lazy row-index filters that each re-execute the input (cache() first). Rows per part are the same for a fixed input order.", wave = "W0" }
split_proportionately = { status = "mismatch", batcher = "Dataset.split_proportionately", note = "Ray materializes once and returns MaterializedDatasets; Batcher parts are lazy and each re-execute the input (cache() first). Rows per part are the same for a fixed input order.", wave = "W0" }
stats = { status = "mismatch", batcher = "Dataset.stats", note = "Ray stats() returns a timing string for the last execution; Batcher stats() executes the query and returns a RunStats object.", wave = "W0" }
std = { status = "mismatch", batcher = "Dataset.std", note = "Batcher std is a single-column sample std; Ray std also takes on=list/None (dict keyed 'std(x)'), ddof=, ignore_nulls=False. On one non-null value Ray returns NaN where Batcher returns null.", wave = "W2" }
streaming_aggregate = { status = "gap", need = "keyed streaming aggregation with a user StreamingAggFn and num_aggregators", wave = "W10" }
streaming_split = { status = "mismatch", batcher = "batcher.ml.streaming_split", note = "Ray returns n DataIterators (one per consumer, equal=, locality_hints); Batcher streaming_split(dataset, world_size, rank=) yields per-rank torch batches in complete rounds and drops a trailing partial round by default.", wave = "W8" }
streaming_train_test_split = { status = "param", batcher = "Dataset.ml.train_test_split", need = "split_type='random' vs 'hash' with hash_column=, streaming (non-materializing) guarantee, ray_remote_kwargs", wave = "W8" }
sum = { status = "param", batcher = "Dataset.sum", need = "on=list or None returning a dict keyed 'sum(x)', ignore_nulls=False", wave = "W2" }
summary = { status = "mismatch", batcher = "Dataset.describe", note = "Ray summary() returns a DatasetSummary organized by data type with override_dtype_agg_mapping; Batcher describe() returns a Dataset with a statistic column and one Float64 column per input column.", wave = "W0" }
take = { status = "mismatch", batcher = ["Dataset.limit", "Dataset.to_pylist"], note = "Ray take(limit=20) is eager and returns rows as dicts; Batcher is lazy. Codemod: .limit(20).to_pylist().", wave = "W0", template = 'take(limit=20) -> self.limit(limit).to_pylist()' }
take_all = { status = "mismatch", batcher = "Dataset.to_pylist", note = "Ray take_all(limit=) is eager and raises if the dataset has more than `limit` rows; Batcher to_pylist() has no guard. Codemod: .to_pylist() (with .limit(limit+1) and a length check when limit is given).", wave = "W0", template = 'take_all(limit=None) -> self.to_pylist(sem.expect(limit, None))' }
take_batch = { status = "mismatch", batcher = ["Dataset.limit", "Dataset.to_numpy"], note = "Ray take_batch(batch_size=20) returns one batch in batch_format='default' ({col: ndarray}); Batcher: .limit(20).to_numpy() returns {col: ndarray}; other batch_format values need to_arrow()/to_pandas().", wave = "W0" }
to_arrow_refs = { status = "out_of_scope", reason = "Ray object-store block references; Batcher moves Arrow batches over Arrow Flight and exposes no object refs" }
to_daft = { status = "canonical", batcher = "Dataset.to_daft" }
to_dask = { status = "gap", need = "export a Dataset to a Dask DataFrame", wave = "W8" }
to_mars = { status = "out_of_scope", reason = "Mars (pymars) interop; Batcher has no Mars bridge and converts through pandas (Dataset.to_pandas)" }
to_modin = { status = "out_of_scope", reason = "Modin wraps pandas; convert through Dataset.to_pandas and modin.pandas.DataFrame" }
to_numpy_refs = { status = "out_of_scope", reason = "Ray object-store block references; Batcher moves Arrow batches over Arrow Flight and exposes no object refs" }
to_pandas = { status = "param", batcher = "Dataset.to_pandas", need = "limit= (raise if the dataset has more rows than limit)", wave = "W2" }
to_pandas_refs = { status = "out_of_scope", reason = "Ray object-store block references; Batcher moves Arrow batches over Arrow Flight and exposes no object refs" }
to_random_access_dataset = { status = "out_of_scope", reason = "experimental actor-served key-value lookup over a sorted dataset; Batcher serves key lookups as a relational Dataset.lookup_join" }
to_spark = { status = "canonical", batcher = "Dataset.to_spark" }
to_tf = { status = "mismatch", batcher = "Dataset.ml.to_tf", note = "Ray to_tf(feature_columns, label_columns) yields (features, labels) tuples with batch_size=1 default; Batcher ds.ml.to_tf yields {column: tensor} dicts and has no feature/label split or additional_columns/feature_type_spec. Dataset.to_tf is being removed in favour of ds.ml.to_tf.", wave = "W8" }
train_test_split = { status = "mismatch", batcher = "Dataset.ml.train_test_split", note = "Ray defaults shuffle=False, so the test set is the last test_size rows in dataset order, and test_size may be an int row count; Batcher assigns rows by a seeded hash (seed=0), takes only a fraction, and is lazy rather than materialized.", wave = "W0" }
union = { status = "canonical", batcher = "Dataset.union" }
unique = { status = "mismatch", batcher = ["Dataset.select", "Dataset.distinct", "Dataset.to_pylist"], note = "Ray unique(column) is eager and returns a list of the column's distinct values (ignore_nulls=False keeps None); Batcher Dataset.distinct deduplicates rows and returns a Dataset. Port as: [r[c] for r in ds.select(c).distinct().to_pylist()].", wave = "W0" }
with_column = { status = "alias", batcher = "Dataset.with_columns", template = 'with_column(column_name, expr) -> self.with_columns(**{column_name: expr})' }
with_columns = { status = "canonical", batcher = "Dataset.with_columns" }
write_bigquery = { status = "gap", need = "BigQuery writer (Batcher reads BigQuery via bt.read.bigquery but cannot write it)", wave = "W13" }
write_clickhouse = { status = "gap", need = "ClickHouse writer with SinkMode and ClickHouseTableSettings", wave = "W13" }
write_csv = { status = "mismatch", batcher = "Dataset.write.csv", note = "Ray write_csv defaults mode=SaveMode.APPEND and writes a directory of CSV files; Batcher defaults mode='overwrite' (replacing existing output) and supports append only for delta/iceberg/hudi/snowflake. Pass mode explicitly; file-sink append is missing.", wave = "W2" }
write_datasink = { status = "gap", need = "write through a user Datasink implementation", wave = "W11" }
write_delta = { status = "mismatch", batcher = "Dataset.write.delta", note = "Both default to append, but Ray write_delta defaults schema_mode='merge' (new columns evolve the table) while Batcher defaults merge_schema=False. Pass merge_schema=True.", wave = "W0" }
write_iceberg = { status = "param", batcher = "Dataset.write.iceberg", need = "mode=SaveMode.UPSERT with upsert_kwargs=, overwrite_filter=, overwrite_kwargs=, snapshot_properties= (Batcher write.iceberg takes mode='append'|'overwrite')", wave = "W13" }
write_images = { status = "gap", need = "image-file writer from an image column (write_images)", wave = "W12" }
write_json = { status = "mismatch", batcher = "Dataset.write.json", note = "Ray write_json defaults mode=SaveMode.APPEND and writes a directory of JSONL files; Batcher defaults mode='overwrite' and cannot append to a file sink. Pass mode explicitly; file-sink append is missing.", wave = "W2" }
write_kafka = { status = "mismatch", batcher = "Dataset.write.kafka", note = "Ray write_kafka is a one-shot batch produce (key_field, key/value serializers); Batcher write.kafka starts a StreamingQuery that must be awaited.", wave = "W10" }
write_lance = { status = "param", batcher = "Dataset.write.lance", need = "mode= (Ray defaults SaveMode.CREATE), min_rows_per_file/max_rows_per_file, data_storage_version, table_id/namespace", wave = "W13" }
write_mongo = { status = "mismatch", batcher = "Dataset.write.mongo", note = "Ray write_mongo(uri, database, collection) inserts; Batcher write.mongo(collection) defaults mode='upsert'. Pass mode='append' and the connection options.", wave = "W13" }
write_numpy = { status = "gap", need = ".npy writer for one column", wave = "W13" }
write_parquet = { status = "mismatch", batcher = "Dataset.write.parquet", note = "Ray write_parquet defaults mode=SaveMode.APPEND into a directory; Batcher defaults mode='overwrite' (replacing existing output) and supports append only for delta/iceberg/hudi/snowflake. Pass mode explicitly; file-sink append is missing.", wave = "W2" }
write_snowflake = { status = "mismatch", batcher = "Dataset.write.snowflake", note = "Ray write_snowflake appends rows to the table; Batcher write.snowflake defaults mode='overwrite'. Pass mode='append'.", wave = "W0" }
write_sql = { status = "mismatch", batcher = "Dataset.write.sql", note = "Ray write_sql(sql, connection_factory) executes a user INSERT statement per row batch through a DB-API connection factory; Batcher write.sql(table, mode=, key_columns=) writes a table by name.", wave = "W13" }
write_tfrecords = { status = "gap", need = "TFRecord writer", wave = "W13" }
write_turbopuffer = { status = "gap", need = "Turbopuffer vector-namespace writer", wave = "W13" }
write_webdataset = { status = "gap", need = "WebDataset (tar shard) writer", wave = "W13" }
zip = { status = "gap", need = "column-wise zip of datasets by row position with a row-count check", wave = "W8" }
