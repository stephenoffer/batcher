# Ray Data 2.58 `DataContext` fields and `ExecutionOptions`, and their Batcher configuration equivalent.

[DataContext]
actor_init_max_retries = { status = "param", batcher = "Config.distributed", need = "number of retries when a UDF actor's constructor fails (actor_max_restarts covers restarts of a started actor)", wave = "W9" }
actor_init_retry_on_errors = { status = "param", batcher = "Config.distributed", need = "per-error-type retry policy (retried error substrings, attempts, backoff) for actor constructor failures; Batcher has retry_on_transient/task_max_retries but no configurable error list", wave = "W9" }
actor_prefetcher_enabled = { status = "out_of_scope", reason = "Ray task scheduling/locality internals (object store, SPREAD strategy, large-arg handling); Batcher placement is governed by DistributedConfig and data bypasses the object store" }
actor_task_retry_on_errors = { status = "param", batcher = "Config.distributed", need = "per-error-type retry policy (retried error substrings, attempts, backoff) for tasks on UDF actors (actor_max_task_retries has no error filter); Batcher has retry_on_transient/task_max_retries but no configurable error list", wave = "W9" }
arrow_fixed_shape_tensor_format = { status = "out_of_scope", reason = "selects the version of Ray's own tensor extension type; Batcher has no tensor-format switch" }
autoscaling_config = { status = "out_of_scope", reason = "Ray actor-pool autoscaler utilization thresholds; Batcher pools size from concurrency=(min, max) and DistributedConfig.autoscale_*" }
batch_to_block_arrow_format = { status = "out_of_scope", reason = "Ray internal backend toggle for its block/tensor implementation; Batcher is Arrow-only in Rust with no alternate block backend" }
custom_execution_callback_classes = { status = "param", batcher = "Config.observability", need = "user execution-lifecycle callback classes invoked on execution start/end (Batcher has streaming listeners and the event bus, no batch-execution callback hook)", wave = "W9" }
dataset_logger_id = { status = "out_of_scope", reason = "Ray dataset logger identifier; Batcher logging is configured through ObservabilityConfig" }
decoding_size_estimation = { status = "out_of_scope", reason = "Ray block sizing and block-count heuristics; Batcher schedules morsels (16,384 rows or 1 MiB) sized by ExecutionConfig" }
default_hash_shuffle_parallelism = { status = "out_of_scope", reason = "Ray hash-shuffle/aggregator actor tuning; Batcher's shuffle is a mergeable partial/combine over Arrow Flight with credit flow control and its own DistributedConfig knobs" }
default_map_logical_memory_enabled = { status = "out_of_scope", reason = "Ray streaming-executor backpressure and resource-reservation internals; Batcher backpressure is credit-based flow control (FlowControlConfig)" }
delta_config = { status = "param", batcher = "Config.distributed", need = "per-error-type retry policy (retried error substrings, attempts, backoff) for Delta commits (DeltaConfig); Batcher has retry_on_transient/task_max_retries but no configurable error list", wave = "W9" }
downstream_capacity_backpressure_ratio = { status = "out_of_scope", reason = "Ray streaming-executor backpressure and resource-reservation internals; Batcher backpressure is credit-based flow control (FlowControlConfig)" }
eager_free = { status = "out_of_scope", reason = "Ray eager object-store freeing of consumed blocks; Batcher buffers live in its own buffer pool (MemoryConfig)" }
enable_arrow_backed_pandas_conversion = { status = "out_of_scope", reason = "Ray internal backend toggle for its block/tensor implementation; Batcher is Arrow-only in Rust with no alternate block backend" }
enable_auto_log_stats = { status = "out_of_scope", reason = "Ray internal debug/metrics toggle for its executor; Batcher observability is configured through ObservabilityConfig" }
enable_dynamic_output_queue_size_backpressure = { status = "out_of_scope", reason = "Ray streaming-executor backpressure and resource-reservation internals; Batcher backpressure is credit-based flow control (FlowControlConfig)" }
enable_fallback_to_arrow_object_ext_type = { status = "out_of_scope", reason = "Ray internal backend toggle for its block/tensor implementation; Batcher is Arrow-only in Rust with no alternate block backend" }
enable_get_object_locations_for_metrics = { status = "out_of_scope", reason = "Ray internal debug/metrics toggle for its executor; Batcher observability is configured through ObservabilityConfig" }
enable_operator_progress_bars = { status = "out_of_scope", reason = "progress-bar rendering detail; Batcher's progress reporter is controlled as a whole by batcher.config.set_progress" }
enable_pandas_block = { status = "out_of_scope", reason = "Ray internal backend toggle for its block/tensor implementation; Batcher is Arrow-only in Rust with no alternate block backend" }
enable_per_node_metrics = { status = "out_of_scope", reason = "Ray internal debug/metrics toggle for its executor; Batcher observability is configured through ObservabilityConfig" }
enable_progress_bar_name_truncation = { status = "out_of_scope", reason = "progress-bar rendering detail; Batcher's progress reporter is controlled as a whole by batcher.config.set_progress" }
enable_progress_bars = { status = "mismatch", batcher = "batcher.config.set_progress", note = "Ray toggles progress bars by assigning a DataContext field; Batcher sets progress reporting with set_progress (or ObservabilityConfig.progress).", wave = "W9" }
enable_rich_progress_bars = { status = "out_of_scope", reason = "progress-bar rendering detail; Batcher's progress reporter is controlled as a whole by batcher.config.set_progress" }
enable_tensor_extension_casting = { status = "out_of_scope", reason = "Ray internal backend toggle for its block/tensor implementation; Batcher is Arrow-only in Rust with no alternate block backend" }
enforce_schemas = { status = "param", batcher = "Config.execution", need = "enforce one declared schema across all blocks/batches of an execution (fail rather than unify)", wave = "W9" }
execution_no_progress_timeout_s = { status = "param", batcher = "Config.distributed", need = "fail an execution that makes no progress for N seconds (autoscale_stall_s governs autoscaling only)", wave = "W9" }
execution_options = { status = "mismatch", batcher = "bt.ExecutionConfig", note = "Ray DataContext.execution_options holds ExecutionOptions (resource_limits, preserve_order, verbose_progress); Batcher's nearest is Config.execution, which has no resource caps or preserve_order switch.", wave = "W9" }
gpu_shuffle_num_actors = { status = "out_of_scope", reason = "Ray GPU shuffle (RMM pool, actor count, spill) tuning; Batcher's device tier is configured through DistributedConfig.gpu_* and routes shuffles differently" }
gpu_shuffle_rmm_pool_size = { status = "out_of_scope", reason = "Ray GPU shuffle (RMM pool, actor count, spill) tuning; Batcher's device tier is configured through DistributedConfig.gpu_* and routes shuffles differently" }
gpu_shuffle_setup_timeout_s = { status = "out_of_scope", reason = "Ray GPU shuffle (RMM pool, actor count, spill) tuning; Batcher's device tier is configured through DistributedConfig.gpu_* and routes shuffles differently" }
gpu_shuffle_spill_memory_limit = { status = "out_of_scope", reason = "Ray GPU shuffle (RMM pool, actor count, spill) tuning; Batcher's device tier is configured through DistributedConfig.gpu_* and routes shuffles differently" }
hash_aggregate_operator_actor_num_cpus_override = { status = "out_of_scope", reason = "Ray hash-shuffle/aggregator actor tuning; Batcher's shuffle is a mergeable partial/combine over Arrow Flight with credit flow control and its own DistributedConfig knobs" }
hash_shuffle_aggregator_health_warning_interval_s = { status = "out_of_scope", reason = "Ray hash-shuffle/aggregator actor tuning; Batcher's shuffle is a mergeable partial/combine over Arrow Flight with credit flow control and its own DistributedConfig knobs" }
hash_shuffle_compression = { status = "out_of_scope", reason = "Ray hash-shuffle/aggregator actor tuning; Batcher's shuffle is a mergeable partial/combine over Arrow Flight with credit flow control and its own DistributedConfig knobs" }
hash_shuffle_operator_actor_num_cpus_override = { status = "out_of_scope", reason = "Ray hash-shuffle/aggregator actor tuning; Batcher's shuffle is a mergeable partial/combine over Arrow Flight with credit flow control and its own DistributedConfig knobs" }
hash_shuffle_reduce_batch_size = { status = "out_of_scope", reason = "Ray hash-shuffle/aggregator actor tuning; Batcher's shuffle is a mergeable partial/combine over Arrow Flight with credit flow control and its own DistributedConfig knobs" }
hash_shuffle_reduce_get_timeout_s = { status = "out_of_scope", reason = "Ray hash-shuffle/aggregator actor tuning; Batcher's shuffle is a mergeable partial/combine over Arrow Flight with credit flow control and its own DistributedConfig knobs" }
iceberg_config = { status = "param", batcher = "Config.distributed", need = "per-error-type retry policy (retried error substrings, attempts, backoff) for Iceberg file writes and catalog calls (IcebergConfig); Batcher has retry_on_transient/task_max_retries but no configurable error list", wave = "W9" }
isolate_read_workers = { status = "out_of_scope", reason = "Ray task scheduling/locality internals (object store, SPREAD strategy, large-arg handling); Batcher placement is governed by DistributedConfig and data bypasses the object store" }
issue_detectors_config = { status = "out_of_scope", reason = "thresholds for Ray's internal hanging/high-memory issue detectors, specific to its streaming executor" }
join_operator_actor_num_cpus_override = { status = "out_of_scope", reason = "Ray hash-shuffle/aggregator actor tuning; Batcher's shuffle is a mergeable partial/combine over Arrow Flight with credit flow control and its own DistributedConfig knobs" }
lance_config = { status = "param", batcher = "Config.distributed", need = "per-error-type retry policy (retried error substrings, attempts, backoff) for Lance fragment reads and writes (LanceConfig); Batcher has retry_on_transient/task_max_retries but no configurable error list", wave = "W9" }
large_args_threshold = { status = "out_of_scope", reason = "Ray task scheduling/locality internals (object store, SPREAD strategy, large-arg handling); Batcher placement is governed by DistributedConfig and data bypasses the object store" }
log_internal_stack_trace = { status = "out_of_scope", reason = "Ray internal debug/metrics toggle for its executor; Batcher observability is configured through ObservabilityConfig" }
max_consecutive_actor_init_deaths = { status = "gap", need = "fail after N consecutive UDF actor constructor deaths", wave = "W9" }
max_errored_blocks = { status = "param", batcher = "Config.distributed", need = "execution-wide tolerance of failed blocks (Batcher has per-UDF max_errored_rows and distributed.on_read_error)", wave = "W9" }
max_hash_shuffle_aggregators = { status = "out_of_scope", reason = "Ray hash-shuffle/aggregator actor tuning; Batcher's shuffle is a mergeable partial/combine over Arrow Flight with credit flow control and its own DistributedConfig knobs" }
max_hash_shuffle_finalization_batch_size = { status = "out_of_scope", reason = "Ray hash-shuffle/aggregator actor tuning; Batcher's shuffle is a mergeable partial/combine over Arrow Flight with credit flow control and its own DistributedConfig knobs" }
max_map_retries = { status = "param", batcher = "Config.distributed", need = "retry count for failed map tasks as its own knob (Batcher exposes one task_max_retries for every task)", wave = "W9" }
max_tasks_in_flight_per_actor = { status = "out_of_scope", reason = "Ray task scheduling/locality internals (object store, SPREAD strategy, large-arg handling); Batcher placement is governed by DistributedConfig and data bypasses the object store" }
memory_usage_poll_interval_s = { status = "out_of_scope", reason = "Ray internal debug/metrics toggle for its executor; Batcher observability is configured through ObservabilityConfig" }
min_hash_shuffle_aggregator_wait_time_in_s = { status = "out_of_scope", reason = "Ray hash-shuffle/aggregator actor tuning; Batcher's shuffle is a mergeable partial/combine over Arrow Flight with credit flow control and its own DistributedConfig knobs" }
min_parallelism = { status = "out_of_scope", reason = "Ray block sizing and block-count heuristics; Batcher schedules morsels (16,384 rows or 1 MiB) sized by ExecutionConfig" }
op_resource_reservation_enabled = { status = "out_of_scope", reason = "Ray streaming-executor backpressure and resource-reservation internals; Batcher backpressure is credit-based flow control (FlowControlConfig)" }
op_resource_reservation_ratio = { status = "out_of_scope", reason = "Ray streaming-executor backpressure and resource-reservation internals; Batcher backpressure is credit-based flow control (FlowControlConfig)" }
override_object_store_memory_limit_fraction = { status = "out_of_scope", reason = "Ray object-store memory budget fraction; Batcher bypasses the object store (budget via MemoryConfig and DistributedConfig.object_store_memory_bytes)" }
pandas_block_ignore_metadata = { status = "out_of_scope", reason = "Ray internal backend toggle for its block/tensor implementation; Batcher is Arrow-only in Rust with no alternate block backend" }
parquet_chunker_target_chunk_size = { status = "out_of_scope", reason = "Ray Parquet writer chunking internal; Batcher write layout is set per write (max_rows_per_file, repartition)" }
pipeline_push_based_shuffle_reduce_tasks = { status = "out_of_scope", reason = "Ray hash-shuffle/aggregator actor tuning; Batcher's shuffle is a mergeable partial/combine over Arrow Flight with credit flow control and its own DistributedConfig knobs" }
print_on_execution_start = { status = "out_of_scope", reason = "Ray internal debug/metrics toggle for its executor; Batcher observability is configured through ObservabilityConfig" }
raise_original_map_exception = { status = "out_of_scope", reason = "toggle between Ray-wrapped and original UDF exceptions, internal to Ray's error reporting; Batcher has no wrapping switch" }
read_op_min_num_blocks = { status = "out_of_scope", reason = "Ray block sizing and block-count heuristics; Batcher schedules morsels (16,384 rows or 1 MiB) sized by ExecutionConfig" }
retried_io_errors = { status = "param", batcher = "Config.distributed", need = "per-error-type retry policy (retried error substrings, attempts, backoff) for filesystem IO errors; Batcher has retry_on_transient/task_max_retries but no configurable error list", wave = "W9" }
retried_map_errors = { status = "param", batcher = "Config.distributed", need = "per-error-type retry policy (retried error substrings, attempts, backoff) for map task failures; Batcher has retry_on_transient/task_max_retries but no configurable error list", wave = "W9" }
s3_try_create_dir = { status = "out_of_scope", reason = "Ray writer toggle to create S3 prefix directories before writing, a filesystem detail internal to Ray's file datasinks" }
scheduling_strategy = { status = "out_of_scope", reason = "Ray task scheduling/locality internals (object store, SPREAD strategy, large-arg handling); Batcher placement is governed by DistributedConfig and data bypasses the object store" }
scheduling_strategy_large_args = { status = "out_of_scope", reason = "Ray task scheduling/locality internals (object store, SPREAD strategy, large-arg handling); Batcher placement is governed by DistributedConfig and data bypasses the object store" }
shuffle_input_batch_bytes = { status = "out_of_scope", reason = "Ray hash-shuffle/aggregator actor tuning; Batcher's shuffle is a mergeable partial/combine over Arrow Flight with credit flow control and its own DistributedConfig knobs" }
streaming_read_buffer_size = { status = "out_of_scope", reason = "Ray block sizing and block-count heuristics; Batcher schedules morsels (16,384 rows or 1 MiB) sized by ExecutionConfig" }
target_max_block_size = { status = "mismatch", batcher = "Config.execution", note = "Ray bounds blocks by bytes (target_max_block_size); Batcher's unit is the morsel, bounded by ExecutionConfig.morsel_rows and morsel_bytes (16,384 rows or 1 MiB by default).", wave = "W9" }
target_min_block_size = { status = "out_of_scope", reason = "Ray block sizing and block-count heuristics; Batcher schedules morsels (16,384 rows or 1 MiB) sized by ExecutionConfig" }
trace_allocations = { status = "out_of_scope", reason = "Ray internal debug/metrics toggle for its executor; Batcher observability is configured through ObservabilityConfig" }
use_arrow_tensor_v2 = { status = "out_of_scope", reason = "Ray internal backend toggle for its block/tensor implementation; Batcher is Arrow-only in Rust with no alternate block backend" }
use_datasource_v2 = { status = "out_of_scope", reason = "Ray internal backend toggle for its block/tensor implementation; Batcher is Arrow-only in Rust with no alternate block backend" }
use_legacy_dataset_ids = { status = "out_of_scope", reason = "Ray internal debug/metrics toggle for its executor; Batcher observability is configured through ObservabilityConfig" }
use_polars = { status = "out_of_scope", reason = "Ray internal backend toggle for its block/tensor implementation; Batcher is Arrow-only in Rust with no alternate block backend" }
use_polars_sort = { status = "out_of_scope", reason = "Ray internal backend toggle for its block/tensor implementation; Batcher is Arrow-only in Rust with no alternate block backend" }
use_push_based_shuffle = { status = "out_of_scope", reason = "Ray hash-shuffle/aggregator actor tuning; Batcher's shuffle is a mergeable partial/combine over Arrow Flight with credit flow control and its own DistributedConfig knobs" }
use_ray_tqdm = { status = "out_of_scope", reason = "progress-bar rendering detail; Batcher's progress reporter is controlled as a whole by batcher.config.set_progress" }
verbose_stats_logs = { status = "out_of_scope", reason = "Ray internal debug/metrics toggle for its executor; Batcher observability is configured through ObservabilityConfig" }
wait_for_min_actors_s = { status = "gap", need = "block until a UDF actor pool reaches its minimum size, with a timeout", wave = "W9" }
warn_on_driver_memory_usage_bytes = { status = "out_of_scope", reason = "Ray internal debug/metrics toggle for its executor; Batcher observability is configured through ObservabilityConfig" }
write_file_retry_on_errors = { status = "param", batcher = "Config.distributed", need = "per-error-type retry policy (retried error substrings, attempts, backoff) for file writes; Batcher has retry_on_transient/task_max_retries but no configurable error list", wave = "W9" }

[ExecutionOptions]
actor_locality_enabled = { status = "out_of_scope", reason = "deprecated by Ray (actor locality is managed internally); Batcher has DistributedConfig.locality_aware_scheduling" }
exclude_resources = { status = "out_of_scope", reason = "deprecated by Ray in favour of label_selector; Batcher reserves resources through DistributedConfig placement rather than subtracting them" }
is_resource_limits_default = { status = "out_of_scope", reason = "introspection helper over ExecutionOptions.resource_limits" }
locality_with_output = { status = "out_of_scope", reason = "Ray object-store locality hint for outputs on the driver node; Batcher streams results over Arrow Flight" }
resource_limits = { status = "param", batcher = "Config.execution", need = "per-execution CPU/GPU/memory/object-store resource caps (ExecutionResources)", wave = "W9" }
validate = { status = "out_of_scope", reason = "internal validation of the ExecutionOptions dataclass; Batcher Config validates in set_config/config_context" }
