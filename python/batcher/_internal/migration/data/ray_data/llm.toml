# Ray Data 2.58 `ray.data.llm` names and their Batcher spelling.

[llm]
ChatTemplateStageConfig = { status = "param", batcher = "batcher.ml.vllm_engine", need = "chat_template= / chat_template_kwargs= override and per-stage batch_size/concurrency/num_cpus/memory (Batcher applies the model's template through chat=True)", wave = "W12" }
DetokenizeStageConfig = { status = "gap", need = "separate CPU detokenize stage with its own batch_size/concurrency/resources", wave = "W12" }
HttpRequestProcessorConfig = { status = "param", batcher = "batcher.ml.http_engine", need = "arbitrary endpoint url with headers=, qps= (per-second rate; Batcher has requests_per_minute), session_factory=, max_retries/base_retry_wait_time_in_s, non-OpenAI payloads", wave = "W12" }
HttpRequestStageConfig = { status = "param", batcher = "batcher.ml.http_engine", need = "per-stage batch_size/concurrency/num_cpus/memory/runtime_env", wave = "W12" }
PrepareMultimodalStageConfig = { status = "param", batcher = "Dataset.ml.generate", need = "multimodal preparation stage options (chat_template_content_format, model_config_kwargs, apply_sys_msg_formatting); Batcher takes image_column= only", wave = "W12" }
Processor = { status = "mismatch", batcher = "Dataset.ml.generate", note = "Ray Processor is a reusable object applied as processor(ds) with preprocess/postprocess row functions around the engine stages; Batcher ds.ml.generate(engine, prompt_column=...) is a per-call Dataset verb with columns rather than row functions.", wave = "W12" }
ProcessorConfig = { status = "param", batcher = "Dataset.ml.generate", need = "one config object carrying batch_size, concurrency, accelerator_type, resources_per_bundle (Batcher takes these as generate() keywords, without resources_per_bundle)", wave = "W12" }
SGLangEngineProcessorConfig = { status = "param", batcher = "batcher.ml.sglang_engine", need = "placement_group_config (TP/PP placement bundles), max_pending_requests, max_concurrent_batches, max_tasks_in_flight_per_actor, log_engine_metrics, dynamic_lora_loading_path, should_continue_on_error, task_type (embed/classify), per-stage configs", wave = "W12" }
ServeDeploymentProcessorConfig = { status = "mismatch", batcher = "batcher.ml.serve_deployment", note = "Ray ServeDeploymentProcessorConfig calls an existing Serve app by app_name/deployment_name (dtype_mapping, request_timeout_s); Batcher serve_deployment builds a new deployment from a predictor factory.", wave = "W12" }
TokenizerStageConfig = { status = "gap", need = "separate CPU tokenize stage with its own batch_size/concurrency/resources", wave = "W12" }
build_processor = { status = "mismatch", batcher = "Dataset.ml.generate", note = "Ray build_processor(config, preprocess=, postprocess=) returns a reusable Processor; Batcher builds nothing reusable: call ds.ml.generate(engine, ...) with an EngineFactory (vllm_engine/sglang_engine/http_engine).", wave = "W12" }
vLLMEngineProcessorConfig = { status = "param", batcher = "batcher.ml.vllm_engine", need = "placement_group_config (TP/PP placement bundles), max_pending_requests, max_concurrent_batches, max_tasks_in_flight_per_actor, log_engine_metrics, dynamic_lora_loading_path, should_continue_on_error, task_type (embed/classify), per-stage configs", wave = "W12" }
