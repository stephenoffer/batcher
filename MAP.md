<!-- GENERATED by tools/gen_map.py — do not edit by hand.
     Module one-liners are quoted from each module's own docstring; run
     `just map` after adding or re-documenting a module. Curated prose
     (disambiguation, routing) is edited in tools/map_notes.md. -->

# Batcher — repository map

**The index of what every file is for.** Grep this file before you search the tree: it answers *where does X live* and *where does new X go* without opening 690 modules. `CLAUDE.md` holds the invariants (the law); this holds the territory.

Covering 613 Python modules across 94 packages and 151 Rust files across 13 crates.

## How to use this map

**Grep this file first.** It is one document holding the docstring summary of every
module in the repo, so `rg -i 'shuffle' MAP.md` answers "where does shuffling live"
in one read instead of a dozen searches that each pull whole files into context.
Open a module only once the map has told you it is the right one.

The three questions the map is built to answer:

| Question | Where to look |
|---|---|
| *Where does this existing thing live?* | Grep the tables below for the concept. |
| *Where does new code go?* | The routing table, then the package one-liner. |
| *What am I allowed to import here?* | The layer number on the package heading. |

Layer numbers are the import matrix from `.claude/rules/architecture.md`: a package
may import anything **strictly lower**, never higher and never sideways. The four
layer-3 subsystems (`kyber`, `carbonite`, `core`, `governance`) are mutually
independent — if two of them need the same helper it goes **down** into `plan`,
`metadata`, `config`, or `_internal`. Copy-paste is the only *wrong* way to share.

## Where does new X go?

`CLAUDE.md` states the rule; this is the lookup table for the cases that come up.

| I am adding… | It goes in | Skill |
|---|---|---|
| A relational operator (join/agg/sort variant) | Rust `bc-ir` + `bc-runtime` (mergeable) → `plan/logical/` → `api` | `add-relational-operator` |
| A scalar or aggregate function | `plan/functions/<family>.py`, surfaced via `api/functions.py` | `add-expression-or-function` |
| A method on `.str`/`.dt`/`.list`/`.struct`/`.json` | `plan/expr_ir/namespaces/` | `add-expression-or-function` |
| An optimizer rewrite | `kyber/rules/<family>.py` or `kyber/rules/extra/<family>.py`, via `@rule` | `add-kyber-optimizer-pass` |
| A cost or cardinality estimate | `kyber/stats/` (cardinality) or `kyber/expr_cost/` (per-row Expr cost) | `add-kyber-optimizer-pass` |
| An IO format or connector | `io/formats/<family>/`, registered as `SourceFormat`/`SinkFormat` | — |
| A spill / memory / backpressure decision | `carbonite/` (policy) + the Rust operator's spill path | — |
| Distributed scheduling of an existing operator | `dist/executors/` — **never** a second semantics | `add-distributed-operator` |
| A new execution tier (JIT/GPU/…) | a `core` `Executor` strategy, not a call-site branch | — |
| A shared plan node, expr, schema, or IR tag | `plan/` (neutral — everyone may import it) | — |
| A user-facing ML/inference surface | `ml/` (layer 6, built *on* the public API) | — |

If the answer is not here and not derivable from a package one-liner, that is a
signal to **ask**, not to invent a home. A misplaced module is how a layer contract
breaks.

## Confusable name clusters

These are the names that collide. Each cluster is genuinely separated — by layer,
by verb, or by exactness — and the table is the separation, so you do not have to
open seven files to rediscover it.

### The seven "stats"

Split by the contract loop's verbs: **Core measures, Kyber decides, Carbonite protects.**

| Path | Layer | Verb |
|---|---|---|
| `plan/stats.py` | 1 | The neutral statistics **algebra** everyone speaks. |
| `plan/source_stats.py` | 1 | The **declaration** a connector makes about a source. |
| `metadata/source_stats_store.py` | 1 | The **durable memory** of stats across queries. |
| `io/stats/` | 2 | **Extracts** cheap stats from connector metadata (footers, catalogs). |
| `core/stats.py` | 3 | **Measures** column statistics from real execution. |
| `kyber/stats/` | 3 | **Estimates** cardinality and column stats to make a decision. |
| `api/source_stats.py` | 5 | **Collects** per-source stats on the conductor's behalf. |

Rule of thumb: producing a number from data → `core`/`io`; consuming one to choose a
plan → `kyber`; the type that carries it → `plan`.

### The four metadata-answer paths

All answer a query without scanning; they differ by *what* they can prove and *who asks*.

| Path | Layer | Answers |
|---|---|---|
| `kyber/shortcuts/` | 3 | The `Facts` provable from a plan, plus the pure derivations over them. |
| `kyber/metadata_filter_count/` | 3 | Filtered counts specifically. Split out only to fit `kyber/`'s file budget. |
| `kyber/metadata_summary/` | 3 | Per-column summaries. Same split, same reason. |
| `kyber/metadata_answer.py` | 3 | Decides *whether* a terminal is provable at all. |
| `api/terminal/metadata_answer/` | 5 | The conductor **using** the above to skip execution. |
| `api/dataset/meta/` | 5 | The user-facing `ds.meta` accessor over all of it. |

Every exact answer returns `None` when it cannot be proved, and `None` means *execute*.
A shortcut is only ever an optimization — never a semantic.

### Families that span crates

Three families are not where a crate doc would lead you. Follow these, not intuition:

- **Sorting** lives in `bc-interp/src/ops/` (`radix_sort`, `sample_sort`, `str_sort`,
  `external_sort`), **not** in `bc-runtime`.
- **Window functions** split: kernels in `bc-runtime/src/window*.rs`, out-of-core
  execution in `bc-interp/src/window_spill.rs`.
- **Spilling** is per-operator, not one module: `bc-runtime/agg/spill.rs`,
  `bc-interp/ops/{mixed_spill, quantile_spill/, external_sort}.rs`,
  `bc-interp/window_spill.rs`, with the budget owned by `bc-resource` and the policy by
  Python `carbonite/`.
- **Filter / project / limit** have no file of their own; they are arms inside
  `bc-interp/src/ops/mod.rs`.

### Same name, different job

| Pair | Difference |
|---|---|
| `bc-io/src/store.rs` vs `bc-transport/src/store.rs` | Object-store URI resolution vs. shuffle-ticket registry. Unrelated. |
| `bc-expr/src/analyze.rs` vs `bc-codegen/src/analyze.rs` | Scheduling predicates over `Expr` vs. JIT-subset validation. |
| `bc-sketches` `countmin` vs `frequent` | *How often is this key* vs. *which keys are heavy*. |
| `minhash` (`eval/str/`) vs `simhash` (`eval/list_ops/`) | Jaccard over shingles vs. cosine over embeddings. |
| `plan/expr_rewrite/` vs `kyber/rules/` | The traversal **mechanism** vs. the rewrite **policy**. |
| `api/merge/` | SQL `MERGE INTO` (upsert) — *not* joining two datasets. |
| `carbonite/transfer/` | The shuffle engine. |
| `dist/spill_breakers/` | Out-of-core sort/join/window ("breaker" = pipeline breaker). |

### Names that read as generic but are not

`kyber/shortcuts` = no-scan answers · `api/orchestration` = the one Kyber→Carbonite→Core
loop · `api/tuning` = *learned* adaptive decisions (not user knobs — those are `config/`)
· `bc-py/src/process.rs` = process-wide singletons · `bc-expr/eval/str/chunk.rs` = the RAG
text splitter · `bc-expr/eval/generate.rs` = `sequence`/`range`, not codegen ·
`bc-runtime/agg/fused.rs` = one pass over `group_ids`, not operator fusion ·
`bc-runtime/keys.rs` = the canonical key encoding every hash path must agree on ·
`bc-arrow/float_ident.rs` = the NaN/±0 identity contract for keys.

## Keeping context small

The engine is larger than any one context window, so treat reading as a budget.

1. **Map first, file second.** Grep `MAP.md`; open only what it names.
2. **Prefer the docstring to the body.** Every module states its own responsibility in
   its first line — that is usually the whole answer.
3. **The oracle is small on purpose.** To learn what an operator *means*, read
   `bc-interp::execute` (the sequential reference), not `par.rs` (2,600 lines of
   scheduling that computes the same thing).
4. **Do not read generated or vendored output.** `MAP.md` itself is generated —
   regenerate with `just map`, never hand-edit.
5. **Trailing `#[cfg(test)]` is most of some Rust files.** `bc-transport/src/lib.rs` is
   92% tests; the line counts in this map already exclude them, so a small number here
   means a small file to read.

## Python control plane — `python/batcher/`

Every package, its architectural layer, and every module it contains. The one-liner is the module's own docstring summary.

### `batcher/` — root

Batcher — a native, JIT-compiling, adaptive data engine.

_(façade only — see the subpackages below)_

### `batcher/api/` — 5 · conductor

The public, fluent, lazy, expression-first API surface.

| module | lines | what it is |
|---|---|---|
| `_join_helpers.py` | 147 | Module-level helpers for `Dataset`: argument coercion and join wiring. |
| `executors.py` | 262 | Execution strategies and their registry (the conductor's wiring). |
| `functions.py` | 284 | Top-level expression constructors re-exported for the public API. |
| `groupby.py` | 594 | `GroupBy` — an in-progress grouped aggregation produced by `Dataset.group_by`. |
| `session.py` | 911 | Session entry points that create `Dataset`s. |
| `source_stats.py` | 260 | Per-source statistics collection for the conductor. |
| `sql_session.py` | 516 | The SQL `Session` — a context binding named tables, Python functions, and a dialect. |
| `stats.py` | 136 | `RunStats` — measured per-operator execution metrics for a `Dataset` run. |

### `batcher/api/adaptive/` — 5 · conductor

Adaptive (intra-query) execution: stage-boundary re-optimization — package façade.

| module | lines | what it is |
|---|---|---|
| `gating.py` | 166 | Whether to run adaptively, and how far to trust an estimate (control plane, `api`). |
| `plan_surgery.py` | 85 | Plan-tree traversal and rewriting for the adaptive loop (control plane, `api`). |
| `staging.py` | 395 | The adaptive stage loop: execute one breaker, re-optimize the rest (control plane, `api`). |

### `batcher/api/dataset/` — 5 · conductor

The `Dataset` builder package.

| module | lines | what it is |
|---|---|---|
| `_build.py` | 456 | Plan-construction helpers behind the thinner `Dataset` methods. |
| `_dedup.py` | 280 | Fuzzy matching — MinHash/SimHash signatures + LSH banding, as relational algebra. |
| `_describe.py` | 212 | Descriptive-statistics helpers behind `Dataset.describe` / `Dataset.null_count`. |
| `_export.py` | 96 | Framework-export helpers behind `Dataset.to_torch` / `to_tf` / `to_torch_dataloader`. |
| `_nulls.py` | 130 | Null handling behind `Dataset.fill_null` / `Dataset.drop_nulls` (the `api` layer). |
| `_window.py` | 105 | Lowering of window expressions into the relational `Window` operator. |
| `callbacks.py` | 143 | Row-callback adapters and the ``@udf`` decorator for the callback transforms. |
| `dq.py` | 495 | The `Dataset.dq` namespace — data-quality expectations with quarantine. |
| `frame.py` | 4449 | `Dataset` — the lazy, immutable, fluent entry point. |
| `ml.py` | 1381 | The `Dataset.ml` namespace — batch inference / embedding / model UDFs. |
| `scd.py` | 372 | The `Dataset.scd` namespace — dimension maintenance from snapshots and change feeds. |

### `batcher/api/dataset/meta/` — 5 · conductor

The `ds.meta` accessor tree (façade) — answer from metadata, execute only when you must.

| module | lines | what it is |
|---|---|---|
| `_facts.py` | 149 | The scaffolding every `ds.meta` accessor stands on: get the facts, or execute instead. |
| `approx.py` | 308 | The `ds.meta.approx` accessor — what the sketches know, and nothing they don't. |
| `checks.py` | 447 | The `ds.meta.col(...).check` accessor — predicate questions answered from the bounds. |
| `column.py` | 427 | The `ds.meta.col(...)` accessor — one column's facts, from the footer when it can be. |
| `frame.py` | 411 | The `ds.meta` accessor — the entry point to every metadata shortcut. |
| `nulls.py` | 167 | The `ds.meta.nulls` accessor — the whole relation's missing-data shape, in one question. |
| `pair.py` | 163 | The `ds.meta.against(other)` accessor — what two relations' footers say about their join. |
| `prove.py` | 98 | Prove a constraint from metadata — a data contract that holds should cost nothing. |
| `schema.py` | 364 | The `ds.meta.schema` accessor — questions about types, which never touch data at all. |
| `storage.py` | 233 | The `ds.meta.storage` accessor — what a scan *would* read, without reading it. |

### `batcher/api/io_namespace/` — 5 · conductor

The unified read/write namespace — `bt.read` (readers) and `ds.write` (sinks).

| module | lines | what it is |
|---|---|---|
| `reader.py` | 1303 | The `bt.read` namespace — typed, per-format dataset readers. |
| `writer.py` | 1168 | The `ds.write` namespace — typed, per-format dataset sinks. |

### `batcher/api/merge/` — 5 · conductor

``MERGE INTO`` — the full SQL merge, composed from relational algebra.

| module | lines | what it is |
|---|---|---|
| `builder.py` | 285 | `MergeBuilder` — the fluent ``MERGE INTO`` surface, and the legacy keyword shorthand. |
| `cdc.py` | 132 | Applying a **change feed** (CDC) to a table — the harder sibling of a keyed upsert. |
| `clauses.py` | 210 | The clause model of a SQL ``MERGE`` — and how a clause names a source vs a target column. |
| `compose.py` | 313 | Compose a full SQL ``MERGE`` out of relational algebra — no new IR, so it distributes. |
| `delta_native.py` | 206 | Native Delta ``MERGE INTO`` — the full clause set, executed as one transaction. |
| `execute.py` | 262 | Executing a MERGE: rewrite only the files that can match, and swap them atomically. |
| `format.py` | 63 | Which format is the table at this path? — the one question a merge can answer by looking. |
| `iceberg_native.py` | 138 | Native Iceberg ``MERGE INTO`` via pyiceberg's `Table.upsert`. |
| `native.py` | 93 | Dispatch a MERGE to the target format's own implementation, when it has one. |
| `plan.py` | 219 | Deciding what a MERGE has to rewrite — the pruning half, with no side effects. |

### `batcher/api/orchestration/` — 5 · conductor

The shared Kyber → Carbonite → Core contract loop for relational plans.

| module | lines | what it is |
|---|---|---|
| `autoconfig.py` | 121 | Zero-config resolution: sense the machine once, and pin it for the query's scope. |
| `run.py` | 550 | The contract loop: Kyber optimizes, Carbonite admits, Core executes, metadata flows back. |

### `batcher/api/security/` — 5 · conductor

The conductor's half of governance: install a policy, and apply it at every read.

| module | lines | what it is |
|---|---|---|
| `_binding.py` | 118 | Where the governance subsystem meets a scan: naming a table, governing it, auditing it. |
| `_context.py` | 101 | The security context: which catalog and principal are in effect for this scope. |

### `batcher/api/streaming/` — 5 · conductor

The streaming-query surface: the public handle, and the launchers behind `ds.write`.

| module | lines | what it is |
|---|---|---|
| `_distributed.py` | 246 | Streaming with the micro-batch fanned across the cluster. |
| `_launch.py` | 147 | The single-node streaming launcher: optimize once, then drive micro-batches. |
| `_query.py` | 200 | The `StreamingQuery` handle users hold, and the registry of running queries. |

### `batcher/api/terminal/` — 5 · conductor

Terminal/materialization operations for `Dataset` — package façade.

| module | lines | what it is |
|---|---|---|
| `_metadata.py` | 443 | Post-execution column-statistics learning (Core measures, Kyber persists). |
| `blob_offload.py` | 121 | Automatic blob offload placement around pipeline breakers. |
| `core.py` | 750 | Terminal/materialization operations for `Dataset`. |
| `distributed_stream.py` | 115 | Distributed streaming terminals — pull a distributed result back in bounded memory. |
| `event_log.py` | 374 | Per-query event log — one JSON document per query (Spark's event-log analog). |
| `gpu_backend.py` | 476 | The opt-in GPU execution backend for supported relational shapes. |
| `map_stream.py` | 144 | Windowed streaming helpers for `map_batches` (UDF) pipelines. |
| `otel.py` | 113 | Emit a query's execution profile as OpenTelemetry spans. |
| `profile.py` | 228 | Profiled terminal execution — the `explain(analyze=True)` / `stats()` engine. |
| `routing.py` | 141 | The `distributed="auto"` routing decision for terminal operations. |

### `batcher/api/terminal/metadata_answer/` — 5 · conductor

Metadata-first terminal resolution — the façade over the answer modules.

| module | lines | what it is |
|---|---|---|
| `_core.py` | 361 | Metadata-first terminal resolution. |
| `aggregate.py` | 270 | Metadata-first resolution of a *keyless aggregate* terminal. |
| `enrich.py` | 110 | Teach the source statistics the facets a source can compute but has not been asked for. |

### `batcher/api/terminal/stream/` — 5 · conductor

Streaming terminal path for `Dataset.iter_batches` — package façade.

| module | lines | what it is |
|---|---|---|
| `dispatch.py` | 411 | Streaming-strategy selection for `Dataset.iter_batches` (control plane, `api`). |
| `watermark.py` | 267 | Watermark-bounded streaming operators for `iter_batches` (control plane, `api`). |

### `batcher/api/tuning/` — 5 · conductor

Conductor adaptive-tuning: activate the learned decisions and close the feedback loops.

| module | lines | what it is |
|---|---|---|
| `decisions.py` | 375 | Conductor-level adaptive-tuning decisions — activate the learned choices, close the loops. |

### `batcher/ml/` — 6 · front-end

ML data plane — actor-pool batch inference, training ingest, and preprocessing.

| module | lines | what it is |
|---|---|---|
| `autobatch.py` | 162 | Adaptive batch-size control for inference — what Ray Data makes you hand-tune. |
| `batch_format.py` | 63 | `batch_format` conversion for `map_batches` — Arrow ↔ numpy / pandas / torch. |
| `converters.py` | 240 | Framework converters — hand Arrow batches to NumPy / PyTorch training loops. |
| `decode.py` | 360 | Decode media columns into tensors — native image decode, Python audio/video. |
| `embed.py` | 246 | Embeddings — compute them (`embed`) and retrieve over them (`vector_search`). |
| `gpu.py` | 790 | Accelerator detection + utilization feedback — the adaptive half of scheduling. |
| `inference.py` | 508 | Actor-pool batch inference — the ML data plane's orchestration layer. |
| `permutation.py` | 187 | The keyed epoch permutation — a shuffled sample order that is computed, not stored. |
| `pipeline.py` | 192 | Multi-stage streaming pipeline with credit-based backpressure (the GPU-feeding moat). |
| `streaming_sampler.py` | 498 | Deterministic, resumable, elastic sample ordering for distributed training. |

### `batcher/ml/llm/` — 6 · front-end

LLM batch inference — the Ray Data LLM competitor (offline text generation).

| module | lines | what it is |
|---|---|---|
| `engines.py` | 390 | LLM engine adapters — the pluggable ``list[str] -> list[str]`` backends. |
| `generate.py` | 303 | LLM batch generation — the columnar half of offline text generation. |
| `packing.py` | 160 | Sequence packing — concatenate tokenized documents into fixed-length training sequences. |
| `structured.py` | 290 | Typed columns out of an LLM — the AI-powered-ETL primitives. |

### `batcher/ml/loader/` — 6 · front-end

Streaming training-data loader — Batcher feeding PyTorch DDP/FSDP/DeepSpeed.

| module | lines | what it is |
|---|---|---|
| `indexed.py` | 209 | The indexed loaders: a deterministic, balanced, resumable global sample order per rank. |
| `lazy.py` | 253 | The lazy path: stream a dataset to torch with no global length and no materialization. |
| `tensors.py` | 102 | Arrow → torch conversion, and moving the result to a device. |

### `batcher/ml/preprocessors/` — 6 · front-end

Preprocessors — sklearn-style fit/transform that reuses Batcher's relational algebra.

| module | lines | what it is |
|---|---|---|
| `base.py` | 151 | The `Preprocessor` contract — sklearn-style fit/transform on a Dataset. |
| `binning.py` | 139 | Binning / discretization preprocessors. |
| `chain.py` | 208 | `Chain` — a sequence of preprocessors fitted and applied as one (sklearn ``Pipeline``). |
| `encoders.py` | 470 | Categorical encoders — fit the category set, transform with a CASE projection. |
| `imputers.py` | 146 | Missing-value imputation — fit a fill value per column, transform with COALESCE. |
| `polynomial.py` | 116 | Polynomial and interaction feature expansion (stateless). |
| `scalers.py` | 498 | Numeric scalers — fit summary statistics, transform with an `Expr` projection. |
| `text.py` | 159 | Feature assembly and text tokenization. |

### `batcher/ml/serving/` — 6 · front-end

Model-serving adapters — call an external inference server from `map_batches`.

| module | lines | what it is |
|---|---|---|
| `base.py` | 133 | Serving clients + the load-once `map_batches` adapter they share. |
| `http.py` | 146 | Generic HTTP/JSON serving adapter — call a REST inference endpoint per batch. |
| `online.py` | 82 | Online (low-latency) serving — a thin Ray Serve adapter over the batch primitives. |
| `torchserve.py` | 56 | TorchServe adapter — batch inference against a TorchServe model endpoint. |
| `triton.py` | 134 | NVIDIA Triton Inference Server adapter — batch inference over HTTP or gRPC. |

### `batcher/_sql/` — 6 · front-end

SQL frontend — run standard SQL over Batcher datasets.

| module | lines | what it is |
|---|---|---|
| `dml.py` | 194 | INSERT / DELETE / UPDATE as pure plan rewrites over a session catalog. |

### `batcher/_sql/parser/` — 6 · front-end

Translate a SQL query (sqlglot AST) into a Batcher `Dataset`.

| module | lines | what it is |
|---|---|---|
| `agg_rewrites.py` | 139 | Aggregate pre-pass rewrites for the SQL translator. |
| `clauses.py` | 286 | SELECT / FROM / JOIN / ORDER clause building for the SQL translator. |
| `core_utils.py` | 286 | Small stateless AST helpers shared across translator theme modules. |
| `from_clause.py` | 441 | FROM / JOIN / UNNEST / VALUES translation for the SQL translator. |
| `grouping.py` | 400 | Grouping, aggregation, and projection mapping for the SQL translator. |
| `grouping_sets.py` | 130 | ROLLUP / CUBE / GROUPING SETS expansion for the SQL translator. |
| `subquery.py` | 483 | Subquery handling and decorrelation for the SQL translator. |
| `subquery_neq.py` | 253 | Correlated ``<>``-residual EXISTS/NOT EXISTS decorrelation (TPC-H q21 shape). |
| `translator.py` | 394 | The `_Translator` skeleton plus the public `sql()` entry point. |
| `udf.py` | 210 | Registered-Python-function support for the SQL translator. |
| `windowing.py` | 461 | Window-function handling for the SQL translator. |

### `batcher/_sql/parser/expressions/` — 6 · front-end

SQL scalar-expression translation — a sqlglot value node becomes an `Expr` (layer 6).

| module | lines | what it is |
|---|---|---|
| `functions.py` | 337 | Named-function dispatch for the SQL translator's scalar path. |
| `json.py` | 47 | SQL JSON extraction — ``json_extract`` / ``json_extract_string`` / ``->`` / ``->>``. |
| `literals.py` | 353 | Literals, temporal handling, dtype mapping, and SQL dispatch tables. |
| `scalar.py` | 466 | Scalar expression dispatch — translate a sqlglot value node into an `Expr`. |

### `batcher/_sql/parser/joins/` — 6 · front-end

Join lowering for the SQL translator — a façade over the join rewrite modules.

| module | lines | what it is |
|---|---|---|
| `lateral.py` | 145 | LATERAL handling for the SQL translator — `UNNEST` and no-FROM lateral subqueries. |
| `theta.py` | 129 | Theta (non-equi) join lowering for the SQL translator. |

### `batcher/dist/` — 4 · backend

`dist` — distributed execution (opt-in, Ray-orchestrated).

| module | lines | what it is |
|---|---|---|
| `executor.py` | 1119 | The distributed executor — the dispatcher. |
| `flight_aggregate.py` | 614 | Distributed aggregation over an Arrow Flight shuffle (object store bypassed). |
| `flight_join.py` | 417 | Distributed hash join over an Arrow Flight shuffle (object store bypassed). |
| `flight_sort.py` | 402 | Distributed sort over an Arrow Flight shuffle (object store bypassed). |
| `flight_window.py` | 218 | Distributed window functions over an Arrow Flight shuffle (object store bypassed). |
| `flight_worker.py` | 1043 | The shared Arrow Flight shuffle worker actor. |
| `shuffle_io.py` | 144 | Arrow IPC shuffle files — the object-store-bypassing data-plane transport. |
| `shuffle_replication.py` | 97 | Shuffle-output replication: turn a worker loss into a re-fetch, not a recompute. |
| `skew.py` | 66 | Learned join-skew: persist the hot join-key values measured by the detection |
| `spill.py` | 429 | Single-node out-of-core aggregation via partition-and-spill, plus the spill |
| `window_stream.py` | 169 | Bounded-memory streaming for a *global* (no-``PARTITION BY``) window. |

### `batcher/dist/adaptive_sizing/` — 4 · backend

Learned execution-time sizing for the distributed executor (façade).

| module | lines | what it is |
|---|---|---|
| `sizing.py` | 272 | Learned execution-time sizing for the distributed executor — measure once, tune next run. |

### `batcher/dist/executors/` — 4 · backend

Per-operator distributed executor implementations.

| module | lines | what it is |
|---|---|---|
| `aggregate.py` | 250 | Distributed aggregation over a disk Arrow-IPC shuffle. |
| `distinct.py` | 51 | Distributed DISTINCT — deduplicate across workers via the aggregate shuffle. |
| `join.py` | 812 | Distributed join: a broadcast path and a co-partition hash-shuffle path. |
| `map.py` | 1258 | Distributed `map_batches` (batch inference) — the Ray Data competitor path. |
| `plan_analysis.py` | 340 | Plan-shape analysis for the distributed dispatcher. |
| `scan_read.py` | 495 | Worker-side scan read primitives — how a distributed worker reads its split slice. |
| `sort.py` | 216 | Distributed sort over a disk Arrow-IPC shuffle. |
| `union.py` | 49 | Distributed UNION — distribute each branch, then concatenate (and dedup). |
| `window.py` | 150 | Distributed window functions over a disk Arrow-IPC shuffle. |
| `write.py` | 130 | Distributed write — parallel data-file writers + one driver-side commit. |

### `batcher/dist/executors/partition_io/` — 4 · backend

Partitioning for the distributed operators — by *source split* and by *key range*.

| module | lines | what it is |
|---|---|---|
| `_sources.py` | 497 | Shared partitioning + post-breaker helpers for the distributed operators. |
| `folds.py` | 87 | Streaming, byte-bounded folds of a shuffle map-side partition. |
| `ranges.py` | 80 | Range partitioning: split rows by *value* into globally ordered buckets. |

### `batcher/dist/executors/ray_runtime/` — 4 · backend

Ray lifecycle, scheduling envelope, autoscaling, and fault policies for the

| module | lines | what it is |
|---|---|---|
| `autoscale_request.py` | 98 | The autoscaler request lifecycle: scale a cluster up for a query, reclaim after. |
| `lifecycle.py` | 430 | Ray lifecycle + single-node fallback for the distributed executor. |
| `metering.py` | 97 | Worker-side metering — the seam that closes the Core→Kyber loop on the distributed path. |
| `policies.py` | 389 | Config-driven fault-tolerance, recovery, and skew policies for the distributed |
| `scaling.py` | 465 | Live cluster topology and the autoscaler request lifecycle. |
| `scheduling.py` | 272 | The metadata-driven scheduling envelope and placement-group machinery. |

### `batcher/dist/fleet/` — 4 · backend

The query-lifetime shuffle fleet and the partitioned intermediate it produces.

| module | lines | what it is |
|---|---|---|
| `_fleet.py` | 476 | A query-lifetime shuffle-actor fleet for the adaptive Flight path. |
| `plan_id.py` | 143 | The per-query shuffle plan id — the fence that keeps concurrent pipelines apart. |
| `source.py` | 122 | A relation whose batches stay partitioned on the shuffle fleet between stages. |

### `batcher/dist/spill_breakers/` — 4 · backend

Out-of-core streaming for the binary/ordering breakers: sort, join, window.

| module | lines | what it is |
|---|---|---|
| `join.py` | 257 | Out-of-core join: co-partition both sides by key, join one bucket pair at a time. |
| `sort.py` | 183 | Out-of-core sort: range-partition into ordered buckets, sort each, yield in key order. |
| `window.py` | 85 | Out-of-core window: grace-partition by the PARTITION BY keys so each bucket holds |

### `batcher/dist/streaming/` — 4 · backend

Distributed streaming heterogeneous execution — overlapped, resource-class stages.

| module | lines | what it is |
|---|---|---|
| `microbatch.py` | 397 | A streaming micro-batch, run across the cluster — one epoch, one transaction. |
| `pipeline.py` | 443 | Distributed streaming heterogeneous inference pipeline (the GPU-feeding moat). |

### `batcher/kyber/` — 3 · subsystem

Kyber — the query optimizer. **Optimization and planning only.**

| module | lines | what it is |
|---|---|---|
| `annotate.py` | 186 | Physical-plan annotation — the `ResourceBounds` Kyber hands Carbonite. |
| `calibration.py` | 320 | Cost-model calibration — turn measured `op_stats` into cost coefficients. |
| `cardinality.py` | 20 | Back-compat shim — cardinality estimation moved to `kyber.stats`. |
| `cost.py` | 297 | Cost model — what will this plan *cost* to run? |
| `cpu_shares.py` | 161 | Adaptive per-task CPU share — turn measured CPU utilization into a `num_cpus`. |
| `learning.py` | 455 | Cross-execution learning — the metadata feedback loop. |
| `metadata_answer.py` | 424 | Answer terminals from metadata alone — Kyber's metadata-first decision layer. |
| `ols.py` | 93 | Shared OLS sufficient statistics for Kyber's learned crossover models. |
| `pass_base.py` | 61 | The optimizer context — shared analysis threaded through every rule. |
| `plan_cache.py` | 338 | Memoize the optimizer — the same query, planned once. |
| `properties.py` | 209 | Physical properties — what a plan node *delivers*, and what its parent *requires*. |
| `registry.py` | 171 | The Kyber rule registry — where rules are discovered and assembled. |
| `rule.py` | 168 | The Kyber rule abstraction — one small, pure unit of optimization. |
| `signature.py` | 154 | Structural plan signatures. |
| `streaming.py` | 194 | Streaming analysis for the optimizer — what is unbounded, and what that forbids. |

### `batcher/kyber/expr_cost/` — 3 · subsystem

Per-row cost of evaluating a scalar `Expr` — the dimension the cost model lacked.

| module | lines | what it is |
|---|---|---|
| `jit.py` | 261 | Which expressions the Cranelift tier compiles — a conservative mirror of `analyze`. |
| `model.py` | 122 | Folding the per-node weights into a per-row cost for a whole expression. |
| `weights.py` | 304 | Per-node evaluation costs, and the traversal that reaches every sub-expression. |

### `batcher/kyber/gpu/` — 3 · subsystem

GPU decisions — Kyber's cost-based accelerator choices, grouped as one family.

| module | lines | what it is |
|---|---|---|
| `adaptive.py` | 79 | Adaptive GPU crossover — learn where the GPU backend starts beating the CPU engine. |
| `policy.py` | 205 | GPU-vs-CPU backend policy — Kyber's cost-based decision of *where* a plan runs. |
| `sizing.py` | 48 | SELECTION-phase rule — size a GPU inference stage's resources. |

### `batcher/kyber/learned_tuning/` — 3 · subsystem

Learned strategy + parameter tuning — self-tuning physical decisions from measured runs.

| module | lines | what it is |
|---|---|---|
| `bandit.py` | 195 | A deterministic UCB1 bandit over a fixed arm set — and the join-strategy choice on it. |
| `crossover.py` | 130 | An OLS two-line crossover — where one algorithm overtakes another, learned from timings. |
| `priors.py` | 217 | Per-signature learned scalars — the priors that seed sizing, pre-aggregation and the re-opt gate. |

### `batcher/kyber/metadata_filter_count/` — 3 · subsystem

EXACT-gated *filtered-count* metadata shortcuts (façade).

| module | lines | what it is |
|---|---|---|
| `answers.py` | 333 | Answer *filtered* counts from metadata — Kyber's EXACT-gated filter-count layer. |

### `batcher/kyber/metadata_summary/` — 3 · subsystem

EXACT-gated per-column *summary* metadata shortcuts (façade).

| module | lines | what it is |
|---|---|---|
| `answers.py` | 116 | Answer a per-column *summary* from metadata — Kyber's EXACT-gated describe layer. |

### `batcher/kyber/optimizer/` — 3 · subsystem

The Kyber optimizer entry point.

| module | lines | what it is |
|---|---|---|
| `driver.py` | 292 | The rule-application engine: phases, fixpoint, and the two levels of fusion. |
| `facade.py` | 344 | The `Optimizer` façade and the module-level entry points. |

### `batcher/kyber/rules/` — 3 · subsystem

Kyber rule modules.

| module | lines | what it is |
|---|---|---|
| `agg_algebra.py` | 159 | Algebraic rewrites over *aggregate* expressions — share a base scan across a |
| `agg_pushdown.py` | 500 | Aggregate-through-join pushdown — pre-aggregate a join side to shrink its input. |
| `algebraic.py` | 486 | Algebraic relational rewrites — small, local, always-correct simplifications. |
| `fusion.py` | 343 | FUSION-phase rewrites — top-N fusion and per-partition top-N (`QUALIFY`). |
| `ordering.py` | 58 | Ordering rewrites — drop work that the input's known order already provides. |
| `projections.py` | 589 | Projection rewrites — collapse stacked projections and prune unread columns. |
| `pushdown.py` | 472 | Predicate pushdown — evaluate filters as early as possible. |
| `selection.py` | 303 | SELECTION-phase rules — cost-based physical algorithm choice. |
| `zonemap_pruning.py` | 307 | Zone-map predicate pruning — eliminate filters provably empty or always-true. |

### `batcher/kyber/rules/extra/` — 3 · subsystem

Extended Kyber rule families.

| module | lines | what it is |
|---|---|---|
| `adaptive_meta.py` | 164 | Adaptive metadata rules — simplifications a provably-EXACT cardinality unlocks. |
| `agg_extra.py` | 475 | Extra aggregate / GROUP BY rewrites — small, local, always-correct simplifications. |
| `agg_rules.py` | 498 | Aggregate rewrites driven by *proven* metadata — uniqueness, constancy, exact counts. |
| `arith_algebra.py` | 316 | Arithmetic algebraic simplification — integer constant reassociation & factoring. |
| `arith_extra.py` | 444 | NORMALIZE-phase arithmetic the other families leave on the table — math-function |
| `boolean_algebra.py` | 499 | NORMALIZE-phase boolean / CASE / COALESCE / NULL simplifications. |
| `casts.py` | 427 | NORMALIZE-phase rules for `CAST` — the shapes a SQL front end and the type-coercion |
| `cse.py` | 172 | Common-subexpression elimination — compute a repeated expression once, not N times. |
| `disjunction_infer.py` | 124 | NORMALIZE-phase implied-predicate inference from a multi-column disjunction. |
| `empty_relation.py` | 124 | Empty-relation folding — turn a provably-empty subtree into the canonical marker. |
| `filter_split.py` | 139 | Cost-based filter splitting — pay an expensive predicate only on surviving rows. |
| `join_extra.py` | 213 | Structural join rewrites — collapse a join whose result is provably fixed. |
| `limit_extra.py` | 315 | LIMIT / top-N rewrites that the existing limit rules leave on the table. |
| `metadata_adaptive.py` | 291 | Metadata-adaptive rewrites — skip or simplify work a proven-EXACT stat makes dead. |
| `nullability.py` | 500 | Schema-driven NULL reasoning — rewrites proved by *declared* nullability. |
| `predicate_infer.py` | 490 | Syntactic predicate inference — simplify a Filter's conjunction from its literals alone. |
| `projection_scan.py` | 335 | Projection, ordering, and scan/schema simplifications — local, always-correct. |
| `pushdown_gaps.py` | 458 | Pushdown gaps — the operators a `Filter`/projection may legally descend past, but didn't. |
| `sargable.py` | 315 | NORMALIZE-phase sargable-predicate normalization — strip arithmetic wrappers so a |
| `setops.py` | 254 | Set-operation rewrites — UNION / DISTINCT structural simplifications. |
| `setops_extra.py` | 296 | Set-operation rewrites that `setops.py` leaves on the table — bag vs set, precisely. |
| `strings.py` | 500 | String-expression rewrites — LIKE despecialization, idempotence collapse, literal folding. |
| `temporal_extra.py` | 490 | NORMALIZE-phase temporal rewrites — the sargability gaps `temporal_sargable` leaves. |
| `temporal_sargable.py` | 221 | NORMALIZE-phase rewrites: temporal extraction predicates → sargable ranges. |
| `topn_limit.py` | 127 | LIMIT / OFFSET rewrites that the base limit rules don't already cover. |
| `window_extra.py` | 360 | Window rewrites — prune keys, frames and functions a window does not actually need. |
| `window_rules.py` | 146 | Window-operator rewrites — canonicalize a `Window`'s keys and prune dead output. |

### `batcher/kyber/rules/extra/conditional/` — 3 · subsystem

NORMALIZE-phase rewrites for the conditional family — CASE / NULLIF / COALESCE / GREATEST /

| module | lines | what it is |
|---|---|---|
| `case.py` | 318 | CASE / NULLIF / COALESCE rewrites. |
| `minmax.py` | 127 | GREATEST / LEAST rewrites. |
| `shared.py` | 137 | Shared guards for the conditional family: purity, type tags, and droppability. |

### `batcher/kyber/rules/extra/join_elim/` — 3 · subsystem

Join elimination — removing a join outright, and the proofs that make it legal.

| module | lines | what it is |
|---|---|---|
| `evidence.py` | 138 | The proofs a join elimination must clear before it may delete or degenerate a join. |
| `rules.py` | 348 | The join-elimination rewrites — outer, self, cartesian, inner-reduction, disjoint-key. |

### `batcher/kyber/rules/extra/runtime_filters/` — 3 · subsystem

Runtime filters and scan-level data skipping — the sideways-information-passing family.

| module | lines | what it is |
|---|---|---|
| `evidence.py` | 475 | The proofs the runtime-filter rules stand on — and nothing else may stand on. |
| `sip.py` | 381 | Sideways information passing — the filters a join implies about its other side. |
| `skipping.py` | 243 | Scan-level data skipping — decide a predicate's *parts* from the column's metadata. |

### `batcher/kyber/rules/joins/` — 3 · subsystem

The join rule family — every rewrite that reshapes a join, in one package.

| module | lines | what it is |
|---|---|---|
| `agg_semijoin.py` | 210 | Sideways information passing into a decorrelated aggregate's input. |
| `order.py` | 578 | Cost-based join reordering — the JOIN_REORDER phase. |
| `projection.py` | 193 | Push a derived projection through a join onto the side it reads, so the join |
| `rewrites.py` | 469 | Join rewrites — change a join's type, push aggregates below it, and prune a side. |

### `batcher/kyber/rules/normalize/` — 3 · subsystem

NORMALIZE-phase whole-tree rewrites, grouped by family.

| module | lines | what it is |
|---|---|---|
| `fold.py` | 275 | Constant folding — evaluate constant sub-expressions at plan time. |
| `ranges.py` | 272 | Predicate → sargable-range rewrites in the NORMALIZE phase. |
| `simplify.py` | 145 | Expression simplification — drop the algebraic identities a rewrite leaves behind. |

### `batcher/kyber/rules/streaming/` — 3 · subsystem

Kyber rule families for streaming (unbounded-input) plans.

| module | lines | what it is |
|---|---|---|
| `watermark.py` | 159 | Pushdown through the watermark-bounded streaming operators. |

### `batcher/kyber/shortcuts/` — 3 · subsystem

EXACT-gated metadata shortcuts (façade) — the answers that need no scan.

| module | lines | what it is |
|---|---|---|
| `approx.py` | 163 | Approximate shortcuts — answers a sketch can give, named so they can never pretend otherwise. |
| `bounds.py` | 170 | Bound shortcuts — a column's extremes, and the facts that follow from them. |
| `checks.py` | 266 | Predicate shortcuts — questions about a column's *values*, answered from its bounds. |
| `distinct.py` | 121 | Cardinality shortcuts — how many distinct values, and whether a column is a key. |
| `facts.py` | 228 | The one place a plan's statistics become *facts* — the substrate every shortcut reads. |
| `joins.py` | 90 | Join shortcuts — the questions two relations' bounds answer about their join. |
| `moments.py` | 54 | Additive shortcuts — a column's total and its average, when something already recorded them. |
| `nulls.py` | 110 | Null-shape shortcuts — how much of a column is missing, and which columns are complete. |
| `ordering.py` | 59 | Ordering shortcuts — what the data is already sorted by, so a sort need not prove it. |
| `rows.py` | 65 | Relation-shape shortcuts — how many rows, how many columns, is there anything there. |
| `storage.py` | 141 | Storage shortcuts — what a scan *would* have read, without reading it. |

### `batcher/kyber/stats/` — 3 · subsystem

`kyber.stats` — cardinality + column-statistics estimation.

| module | lines | what it is |
|---|---|---|
| `aggregate_columns.py` | 184 | Aggregate output column statistics — the values a grouped/global aggregate produces. |
| `columns.py` | 411 | Per-operator column-statistics propagation. |
| `constants.py` | 76 | When a *computed* column is provably a constant — the one projection that keeps EXACT. |
| `derived.py` | 148 | Bounds through a monotonic arithmetic projection — the one *non-constant* computed |
| `estimator.py` | 1166 | `StatsEstimator` — propagate `RelStats` (rows + column stats) through a plan. |
| `skew.py` | 89 | Join-key skew that Kyber already knows — no detection pass, no prior run of the shape. |

### `batcher/kyber/stats/selectivity/` — 3 · subsystem

Predicate selectivity — the fraction of rows a `Filter` keeps.

| module | lines | what it is |
|---|---|---|
| `combine.py` | 415 | Composing leaf selectivities into a whole-predicate estimate. |
| `leaves.py` | 397 | Leaf predicate selectivity — one estimate per non-composite predicate. |
| `scalars.py` | 185 | Scalar and column-statistic primitives shared by every selectivity estimator. |

### `batcher/carbonite/` — 3 · subsystem

Carbonite — the resource manager. **Resources, memory, and flow control only.**

| module | lines | what it is |
|---|---|---|
| `base.py` | 102 | Policy seams for the Carbonite resource manager. |
| `cache.py` | 233 | The result cache — a memory-bounded LRU of materialized query results. |
| `manager.py` | 483 | The Carbonite resource manager entry point. |
| `policies.py` | 523 | Bootstrap resource policies — the permissive single-node defaults. |
| `spill.py` | 394 | Tiered spill storage — keep large state alive under bounded memory, at any scale. |

### `batcher/carbonite/memory/` — 3 · subsystem

Carbonite memory governance: the buffer pool, pressure sensing, estimation.

| module | lines | what it is |
|---|---|---|
| `estimator.py` | 61 | Per-operator memory estimation — what envelope a plan needs to run in memory. |
| `learned.py` | 307 | Learned per-family memory model — turn measured `m_peak_bytes` into sizing. |
| `pool.py` | 173 | The buffer pool — Carbonite's reserve-before-allocate accounting. |
| `pressure.py` | 519 | Live memory-pressure sensing — Carbonite's view of how full RAM is. |

### `batcher/carbonite/resilience/` — 3 · subsystem

Carbonite fault tolerance: Spark-style recompute-from-lineage on worker loss.

| module | lines | what it is |
|---|---|---|
| `lineage.py` | 76 | Shuffle lineage — how to recompute an output a lost worker produced. |
| `preemption.py` | 218 | Spot-preemption detection so the engine drains proactively, not reactively. |
| `recovery.py` | 97 | Shuffle recovery — the recompute-on-failure coordination loop. |
| `replication.py` | 83 | Where each mapper's shuffle output is copied, so a lost worker costs a fetch not a recompute. |
| `speculative.py` | 160 | Straggler mitigation — speculative backup tasks for shuffle barriers. |

### `batcher/carbonite/transfer/` — 3 · subsystem

Carbonite data transfer: the standalone, locality-aware shuffle engine.

| module | lines | what it is |
|---|---|---|
| `locality.py` | 77 | Transfer-mode selection — move a partition the cheapest way its placement allows. |
| `placement.py` | 82 | Locality-aware reducer placement — put a reducer where its data already is. |
| `server.py` | 359 | The node-local Arrow Flight shuffle server — Carbonite's transfer endpoint. |
| `session.py` | 425 | The ShuffleSession — Carbonite's operator-agnostic data-movement engine. |
| `tls.py` | 86 | Load the shuffle TLS material a worker presents and trusts. |

### `batcher/core/` — 3 · subsystem

Core — the adaptive executor. **Execution and adaptation only.**

| module | lines | what it is |
|---|---|---|
| `base.py` | 83 | The execution-strategy seam: one `Executor` Protocol, one `ExecutionContext`. |
| `executor.py` | 212 | The Core local executor. |
| `gpu_plan.py` | 350 | Translate a linear Batcher plan to a GPU dataframe execution (cuDF) — many ops, not one. |
| `gpu_transform.py` | 183 | GPU-accelerated relational transform kernels (the compute core of a GPU backend). |
| `mergeable.py` | 169 | The one running fold over the mergeable aggregate algebra. |
| `runtime.py` | 67 | Process-wide runtime services for Core (the default MetadataHub). |
| `scan_only.py` | 125 | A bare scan needs no engine — the reader has already produced the plan's output. |
| `stats.py` | 153 | Column-statistics measurement — Core's lane. |
| `streaming.py` | 418 | Streaming (incremental) aggregation — bounded-memory group-by over a source. |
| `streaming_query.py` | 451 | The streaming-query engine — the micro-batch loop behind a unified `ds.write`. |
| `streaming_runner.py` | 169 | How one micro-batch gets run — the seam between the loop and where the work happens. |

### `batcher/core/udf/` — 3 · subsystem

Execution of pipelines containing `map_batches` (opaque Python/ML operators).

| module | lines | what it is |
|---|---|---|
| `call.py` | 106 | The per-batch `map_batches` call boundary (Core, layer 3). |
| `execute.py` | 418 | Execution of pipelines containing `map_batches` (opaque Python/ML operators). |
| `processes.py` | 164 | The warm, shared process pool that runs CPU-bound `map_batches` UDFs off the GIL. |
| `strategy.py` | 394 | How a `map_batches` `fn` is run: threads vs processes, and the per-batch row count. |
| `stream.py` | 445 | Streaming, stage-overlapped execution of a linear `map_batches` chain. |

### `batcher/governance/` — 3 · subsystem

Governance — who may read which rows and columns, and through what mask.

| module | lines | what it is |
|---|---|---|
| `audit.py` | 89 | `GovernanceEvent` — the record of one authorization decision. |
| `catalog.py` | 368 | `SecurityCatalog` — the declared policies, and how they resolve for a principal. |
| `enforce.py` | 167 | `enforce` — rewrite a plan so a principal can only read what the catalog allows. |
| `filters.py` | 90 | Declarative, picklable row-filter factories for attribute-based row access. |
| `lineage.py` | 200 | Column-level lineage — which source columns each output column is derived from. |
| `masks.py` | 112 | Declarative, picklable column-mask factories. |
| `policy.py` | 149 | The policy objects a `SecurityCatalog` holds: grants, column masks, row filters. |
| `principal.py` | 92 | `Principal` — who is running the query. |

### `batcher/io/` — 2 · neutral IO

`io` — data sources and sinks.

| module | lines | what it is |
|---|---|---|
| `_backend.py` | 435 | The `pyarrow.fs`-backed filesystem façade every IO source and sink talks to. |
| `_concurrent.py` | 58 | Concurrent per-file reads — the shared fan-out for footer/header stats and file bytes. |
| `_file_cache.py` | 137 | Local-SSD read-through file cache (the Disk-Cache analog) for remote reads. |
| `catalog.py` | 125 | Unified lakehouse catalog resolver. |
| `credentials.py` | 178 | Credential resolution for connectors, plus Databricks Unity Catalog vending. |
| `detect.py` | 144 | Format auto-detection for the generic `read(path, format=None)` entry point. |
| `filesystem.py` | 370 | Filesystem resolution for IO sources and sinks — one cloud-agnostic backend. |
| `interop.py` | 332 | Framework-interop ingestion — build a `Source` from a foreign object. |
| `manifest.py` | 141 | Write results — the manifest a sink returns and a commit consumes. |
| `predicate.py` | 370 | Predicate translation for source-side pushdown. |
| `sink.py` | 104 | Data sinks — persisting query results. |

### `batcher/io/base/` — 2 · neutral IO

Template-Method base classes for file-backed sources and sinks.

| module | lines | what it is |
|---|---|---|
| `_readahead.py` | 199 | Order-preserving, **byte-bounded** read-ahead over a sequence of files. |
| `_tolerance.py` | 79 | The per-file error policy a `FileSource` read applies to an unreadable file. |
| `_transient.py` | 145 | Retry for the object-store failures that are worth retrying, and only those. |
| `sink.py` | 391 | `FileSink` — the Template-Method base every file-format writer subclasses. |
| `source.py` | 657 | `FileSource` — the Template-Method base every file-format reader subclasses. |

### `batcher/io/formats/` — 2 · neutral IO

`io.formats` — formats grouped by family, behind a registry.

| module | lines | what it is |
|---|---|---|
| `base.py` | 78 | Format contracts and registries — the seam new IO formats plug into. |

### `batcher/io/formats/lakehouse/` — 2 · neutral IO

`io.formats.lakehouse` — table-format connectors behind the format registry.

| module | lines | what it is |
|---|---|---|
| `_arrow.py` | 83 | Normalize the Arrow types a lakehouse client hands back into the ones the engine speaks. |
| `_staging.py` | 140 | Staged-file writes for the Iceberg sink. |
| `delta_sharing.py` | 299 | Delta Sharing format — read a shared table directly into Arrow. |
| `hudi.py` | 343 | Apache Hudi format — read-only via `hudi` (hudi-rs). |
| `maintenance.py` | 157 | Table maintenance — compaction, clustering, and reclamation, as transactions. |

### `batcher/io/formats/lakehouse/delta/` — 2 · neutral IO

`io.formats.lakehouse.delta` — the Delta Lake connector.

| module | lines | what it is |
|---|---|---|
| `_commit.py` | 427 | The metadata-only Delta commit: register worker-written files, move no data. |
| `_predicate.py` | 174 | Rendering an expression for delta-rs: as partition filters, or as SQL. |
| `_snapshot.py` | 476 | One cached read of a Delta table's `_delta_log`, shared by every metadata question. |
| `maintenance.py` | 172 | Delta table maintenance: OPTIMIZE, ZORDER, VACUUM, and log checkpointing. |
| `sink.py` | 327 | Writing a Delta Lake table: workers write final data files, the driver commits metadata. |
| `source.py` | 473 | Reading a Delta Lake table: log-driven file skipping, time travel, and CDF. |
| `stream.py` | 136 | Reading a Delta table as an unbounded stream via its Change Data Feed. |

### `batcher/io/formats/lakehouse/iceberg/` — 2 · neutral IO

`io.formats.lakehouse.iceberg` — the Apache Iceberg connector.

| module | lines | what it is |
|---|---|---|
| `_common.py` | 41 | Shared helpers for the Iceberg connector: the dependency gate and write tokens. |
| `_manifest.py` | 97 | Iceberg's per-file metrics, normalized into the add-action layout the engine prunes with. |
| `maintenance.py` | 125 | Iceberg table maintenance: snapshot expiry, and an honest refusal to compact. |
| `sink.py` | 240 | Writing an Iceberg table: workers stage data files, the driver commits one snapshot. |
| `source.py` | 487 | Reading an Iceberg table: manifest-level file skipping, time travel, incremental scans. |

### `batcher/io/formats/ml/` — 2 · neutral IO

ML / array formats (NumPy, TFRecord, WebDataset, HDF5, Zarr) + training shards

| module | lines | what it is |
|---|---|---|
| `hdf5.py` | 124 | HDF5 format — array-dataset read via `h5py`, sliced to Arrow. |
| `numpy.py` | 90 | NumPy ``.npy`` / ``.npz`` source — arrays as Arrow columns. |
| `point_cloud.py` | 277 | Point-cloud sources — LiDAR / depth sensor frames as Arrow columns. |
| `shards.py` | 197 | Sharded training dataset — fixed-size Arrow-IPC shards + a JSON index. |
| `tensor.py` | 98 | Fixed-shape tensor columns — multi-dimensional arrays as one Arrow column. |
| `tfrecord.py` | 130 | TFRecord format — TensorFlow record stream → Arrow via manual framing. |
| `webdataset.py` | 81 | WebDataset format — `.tar` shard reader via stdlib `tarfile` (core, no extra). |
| `zarr.py` | 117 | Zarr format — chunked array read via `zarr`, chunk-parallel to Arrow. |

### `batcher/io/formats/multimodal/` — 2 · neutral IO

Multimodal sources — images/audio/video/embeddings as queryable Arrow columns.

| module | lines | what it is |
|---|---|---|
| `_batching.py` | 94 | How a media read is cut into file-batches — bounded by file count *and* bytes. |
| `_mime.py` | 68 | MIME sniffing for media files — magic bytes first, extension as the fallback. |
| `_pruning.py` | 114 | Prune media files at plan time using the metadata a listing already knows. |
| `audio.py` | 63 | Audio source — list audio files + header-only sample_rate/channels/duration. |
| `blob.py` | 166 | Blob-by-reference: offload large per-row payloads to a content-addressed store. |
| `embeddings.py` | 172 | Embedding source — vector files (.npy / .parquet) → an Arrow embedding column. |
| `images.py` | 59 | Image source — list image files + header-only width/height/mode. |
| `media.py` | 483 | Multimodal media source base — list files, never decode pixels/frames. |
| `video.py` | 70 | Video source — list video files + header-only fps/frames/width/height/duration. |

### `batcher/io/formats/nosql/` — 2 · neutral IO

`io.formats.nosql` — connectors for NoSQL / operational data stores.

| module | lines | what it is |
|---|---|---|
| `base.py` | 338 | Shared shape for NoSQL / operational-store scan sources. |
| `cassandra.py` | 200 | Cassandra / ScyllaDB connector — token-range parallel scan to Arrow. |
| `couchbase.py` | 178 | Couchbase connector — Columnar (analytics) SDK to Arrow. |
| `dynamodb.py` | 263 | DynamoDB connector — native parallel scan to Arrow. |
| `elasticsearch.py` | 251 | Elasticsearch connector — ES|QL Arrow output with sliced-scroll splits. |
| `hbase.py` | 144 | HBase connector — region-range partitioned scan to Arrow via happybase. |
| `mongo.py` | 259 | MongoDB connector — Arrow-native read via ``pymongoarrow``, batch-upsert write. |
| `neo4j.py` | 168 | Neo4j connector — Bolt driver, SKIP/LIMIT partitioned read to Arrow. |
| `redis.py` | 167 | Redis connector — slot-partitioned SCAN to Arrow. |

### `batcher/io/formats/robotics/` — 2 · neutral IO

Robotics / ADAS log formats — the containers a vehicle or robot records into.

| module | lines | what it is |
|---|---|---|
| `_pushdown.py` | 141 | Translate a pushed predicate into the message-index filters a robot log supports. |
| `mcap.py` | 344 | MCAP — the container robotics and ADAS logs are recorded in. |
| `mdf.py` | 220 | ASAM MDF4 (``.mf4``) — the measurement format vehicle data is logged in. |

### `batcher/io/formats/semistructured/` — 2 · neutral IO

`io.formats.semistructured` — record-shaped data whose schema must be inferred.

| module | lines | what it is |
|---|---|---|
| `json.py` | 463 | JSON format — newline-delimited (line) JSON read + write. |
| `logs.py` | 86 | Log format — line-delimited text logs read as raw lines (core, no extra). |
| `msgpack.py` | 100 | MessagePack format — row-oriented read + write via `ormsgpack`, to Arrow. |
| `protobuf.py` | 114 | Protobuf format — length-delimited message stream → Arrow via `protarrow`. |
| `xml.py` | 61 | XML format — Arrow-native nested read via `xml2arrow`. |

### `batcher/io/formats/sql/` — 2 · neutral IO

`io.formats.sql` — SQL / data-warehouse connectors, behind the registry.

| module | lines | what it is |
|---|---|---|
| `_common.py` | 195 | Shared helpers for SQL/warehouse sources — query rewriting and import guards. |
| `adbc.py` | 415 | ADBC / FlightSQL source + sink — Arrow-native database connectivity. |
| `bigquery.py` | 269 | BigQuery source — multi-stream Arrow reads via the Storage Read API. |
| `clickhouse.py` | 175 | ClickHouse source — Arrow reads via clickhouse-connect. |
| `connectorx.py` | 187 | ConnectorX source — the parallel relational reader for the long tail. |
| `databricks.py` | 232 | Databricks source — direct lakehouse read, warehouse fallback. |
| `dbapi.py` | 416 | DB-API 2.0 (PEP 249) source — the universal fallback for any Python driver. |
| `odbc.py` | 163 | ODBC source — Arrow reads via turbodbc, for the enterprise tail. |
| `partition.py` | 113 | Range partitioning — turning one big table read into N parallel queries. |
| `snowflake.py` | 256 | Snowflake source + sink — one query submission, N shippable result chunks. |
| `uri.py` | 360 | Connection-URI parsing — one industry-standard URI, routed to the right backend. |

### `batcher/io/formats/streaming/` — 2 · neutral IO

`io.formats.streaming` — broker + incremental-file sources, behind the registry.

| module | lines | what it is |
|---|---|---|
| `autoloader.py` | 241 | Incremental file discovery — the Auto Loader analog (Databricks ``cloudFiles``). |
| `broker.py` | 317 | Shared base for row/message-based streaming brokers (Kafka, Kinesis, …). |
| `dev.py` | 176 | Development streaming sources — `rate` and `socket` (Spark parity). |
| `eventhubs.py` | 125 | Azure Event Hubs broker source — one Split per partition, via ``azure-eventhub``. |
| `kafka.py` | 176 | Kafka broker source — one Split per topic-partition, exactly-once commits. |
| `kinesis.py` | 190 | Kinesis broker source — one Split per shard, via ``boto3`` shard iterators. |
| `pubsub.py` | 97 | Google Cloud Pub/Sub broker source — subscription pull batches. |
| `pulsar.py` | 145 | Apache Pulsar broker source — one Split per partition, via ``pulsar-client``. |
| `seen_store.py` | 134 | A durable "seen-file" key-value store, backed by stdlib ``sqlite3``. |
| `sinks.py` | 269 | Streaming sinks — per-micro-batch writers for the streaming-query engine. |

### `batcher/io/formats/streaming/checkpoint/` — 2 · neutral IO

Streaming-query checkpointing — offset log, commit log, and state store.

| module | lines | what it is |
|---|---|---|
| `logs.py` | 124 | Durable offset + commit logs for streaming-query checkpointing. |
| `recovery.py` | 48 | The recovery decision at streaming-query start. |
| `state_store.py` | 67 | Durable snapshots of a streaming query's running aggregation state. |
| `store.py` | 64 | `CheckpointStore` — the offset log, commit log, and state store under one dir. |

### `batcher/io/formats/structured/` — 2 · neutral IO

`io.formats.structured` — formats that already carry a schema, behind the registry.

| module | lines | what it is |
|---|---|---|
| `_parquet_native.py` | 147 | Native Rust Parquet reads (via `bc_io` through `batcher._native`), with PyArrow fallback. |
| `arrow_ipc.py` | 154 | Arrow IPC / Feather format — zero-conversion read + write via `pyarrow.ipc`. |
| `avro.py` | 329 | Avro format — row-oriented read + write via `fastavro`, assembled to Arrow. |
| `csv.py` | 386 | CSV format — lazy read + write via pyarrow, with byte-range splits. |
| `excel.py` | 103 | Excel format — read-only sheet ingestion via `python-calamine`, to Arrow. |
| `lance.py` | 245 | Lance format — columnar, random-access read + write via `pylance` (lance). |
| `orc.py` | 305 | ORC format — lazy, projection-pushdown read + write via `pyarrow.orc`. |

### `batcher/io/formats/structured/parquet/` — 2 · neutral IO

Parquet — lazy projection/predicate read + write, plus the dataset reader.

| module | lines | what it is |
|---|---|---|
| `_native_stream.py` | 142 | Native-reader streaming for `ParquetSource._iter_file`, and the rule for when to use it. |
| `dataset.py` | 281 | `ParquetDatasetSource` — a Hive-partitioned Parquet directory tree, read at scale. |
| `sink.py` | 167 | `ParquetSink` — the Parquet writer. |
| `source.py` | 472 | `ParquetSource` — lazy projection/predicate read of one or more Parquet files. |

### `batcher/io/formats/unstructured/` — 2 · neutral IO

`io.formats.unstructured` — sources with no schema beyond bytes or text.

| module | lines | what it is |
|---|---|---|
| `binary.py` | 163 | Binary-blob source — whole files as ``{uri, bytes, size, mime}`` rows. |
| `documents.py` | 143 | Document format — PDF text extraction via `pypdf`, to Arrow. |
| `text.py` | 179 | Plain-text source — one row per line or one row per whole file. |

### `batcher/io/schema/` — 2 · neutral IO

`io.schema` — reconciling schemas that disagree across the files of one source.

| module | lines | what it is |
|---|---|---|
| `evolution.py` | 255 | Schema reconciliation for multi-file reads — column union, type promotion, drift. |

### `batcher/io/source/` — 2 · neutral IO

Source connectors — the façade over the source implementation modules.

| module | lines | what it is |
|---|---|---|
| `base.py` | 184 | The `Source` protocol — the contract every connector satisfies. |
| `inmemory.py` | 476 | `InMemorySource` — a relation already materialized as Arrow record batches. |
| `inmemory_stats.py` | 266 | Lazy EXACT column statistics over an immutable in-memory Arrow relation. |
| `iterator.py` | 186 | `IteratorSource` — a streaming relation backed by a re-iterable batch factory. |
| `materialized.py` | 70 | `MaterializedSource` — a distributed stage's result, left partitioned on disk. |
| `read.py` | 160 | The neutral read helpers every executor calls a `Source` through. |

### `batcher/io/splits/` — 2 · neutral IO

Splits — independently-readable, picklable slices of a source.

| module | lines | what it is |
|---|---|---|
| `base.py` | 244 | The `Split` protocol and the whole-source fallback. |
| `file.py` | 270 | File-locator splits — a whole file, an IPC stream file, or a byte range of one. |
| `parquet.py` | 412 | Parquet-dataset split locators — row groups, the footer cache, the fragment index. |

### `batcher/io/stats/` — 2 · neutral IO

`io.stats` — extract cheap `SourceStatistics` from connector metadata.

| module | lines | what it is |
|---|---|---|
| `columnar_footer.py` | 377 | Footer-derived statistics for columnar formats (Parquet, ORC, Arrow IPC). |
| `file_identity.py` | 142 | A cheap identity token for a file, so a metadata cache cannot serve a stale answer. |
| `file_skipping.py` | 282 | Manifest-driven file skipping — turn a pushed predicate into a surviving-file set. |
| `free_counts.py` | 59 | Free row counts from file headers — metadata that costs one header read. |
| `key_pruning.py` | 261 | Key-driven file pruning — the copy-on-write MERGE's "which files must I rewrite?". |
| `lakehouse_manifest.py` | 187 | Manifest-derived statistics for lakehouse tables (Delta, Iceberg). |
| `parquet_manifest.py` | 150 | Per-**file** Parquet bounds, in the add-action layout, for file-level pruning. |
| `pruning.py` | 125 | Row-group / file pruning metadata mined from Parquet footers. |
| `sortedness.py` | 123 | Prove — never assume — that a Parquet dataset is globally sorted by a key. |
| `sql_catalog.py` | 78 | Catalog-derived statistics for SQL warehouses. |

### `batcher/observe/` — 2 · neutral sinks

Observability sinks — the terminal reporter, the activity store, and the web dashboard.

| module | lines | what it is |
|---|---|---|
| `console.py` | 446 | The terminal face of the engine — a live progress bar plus structured status lines. |
| `control.py` | 174 | Turning the sinks on and off — the one place that owns observability's global state. |
| `dag.py` | 299 | The executed plan as a laid-out graph — nodes, edges, and their measured stats. |
| `server.py` | 215 | The Batcher UI — a local web dashboard for queries, plans, metrics, and logs. |
| `store.py` | 431 | The bounded in-memory record of recent engine activity — the UI's data model. |
| `system.py` | 176 | The host and engine the queries are running on — the dashboard's hardware panel. |
| `theme.py` | 144 | Terminal capability detection, the color ramp, and the glyph set — the console's look. |

### `batcher/observe/analytics/` — 2 · neutral sinks

Cross-run analysis — percentiles, trends, rollups, comparisons, and a health verdict.

| module | lines | what it is |
|---|---|---|
| `comparison.py` | 95 | Run-against-run comparison — what changed between two executions. |
| `health.py` | 118 | The engine's current verdict — a small set of checks over recent runs. |
| `pipeline.py` | 206 | The per-pipeline report — its runs, its steps, its plan graph, and its run grid. |
| `rollups.py` | 102 | Grouping across runs — cost by operator kind, and failures by cause. |
| `series.py` | 92 | Distributions and time series over many runs — percentiles and session throughput. |

### `batcher/observe/insights/` — 2 · neutral sinks

Automatic findings for one run — what is wrong, the evidence, and what to do.

| module | lines | what it is |
|---|---|---|
| `dataflow.py` | 157 | Findings about rows: how many were read, discarded, or multiplied. |
| `kinds.py` | 110 | What a finding is, the thresholds rules compare against, and prose helpers. |
| `planning.py` | 166 | Findings about how the work was distributed and how well it was predicted. |
| `resources.py` | 253 | Findings about the machine: memory, spill, and CPU the run did or did not get. |

### `batcher/plan/` — 1 · contract

`plan` — the shared plan IR and inter-layer contracts.

| module | lines | what it is |
|---|---|---|
| `bloom_index.py` | 110 | `BloomIndex` — a data-skipping membership index over a column's values. |
| `feedback.py` | 106 | Execution feedback contract: Core → Kyber. |
| `ids.py` | 10 | Stable identifiers used across plans and feedback. |
| `ir_specs.py` | 102 | The shared sub-document shapes of the JSON IR — group keys, aggregates, sort keys. |
| `ir_tags.py` | 116 | The JSON IR vocabulary — the single Python home for the wire-contract tags. |
| `physical.py` | 117 | `PhysicalPlan` — what Kyber emits and Core executes. |
| `resource.py` | 218 | Resource contracts between Kyber (optimizer) and Carbonite (resource manager). |
| `schema.py` | 71 | `SchemaRef` — a thin wrapper making `pyarrow.Schema` the source of truth. |
| `source_stats.py` | 125 | `plan.source_stats` — what a connector declares about a source, cheaply. |
| `stats.py` | 257 | `plan.stats` — the neutral statistics algebra shared across every layer. |
| `visitor.py` | 183 | Shared traversal for `LogicalPlan` trees. |

### `batcher/plan/expr_ir/` — 1 · contract

The scalar expression algebra.

| module | lines | what it is |
|---|---|---|
| `audio.py` | 221 | The `.audio` expression namespace — lazy, batch-level audio decode. |
| `constructors.py` | 322 | Module-level expression constructors (the user-facing entry points). |
| `core.py` | 4419 | The scalar expression base class and its core IR nodes. |
| `fn_names.py` | 150 | The scalar-function vocabulary — the documented home for `fn` discriminators. |
| `func_nodes.py` | 295 | IR node classes built by the accessor namespaces (`.str`/`.dt`/`.list`/…). |
| `image.py` | 291 | The `.image` expression namespace — lazy, batch-level image decode. |
| `node_base.py` | 203 | Declarative base for the scalar `Expr` IR nodes — kills the `to_ir()` boilerplate. |
| `nodes.py` | 498 | Leaf IR nodes the `Expr` base class does not construct. |
| `render.py` | 189 | A readable ``repr`` for the scalar `Expr` tree. |
| `video.py` | 74 | The `.video` expression namespace — lazy, batch-level video decode. |
| `walk.py` | 470 | Structural traversals over the expression tree. |

### `batcher/plan/expr_ir/namespaces/` — 1 · contract

Accessor namespaces (`.str`/`.dt`/`.list`/`.struct`/`.json`) — package façade.

| module | lines | what it is |
|---|---|---|
| `_bind.py` | 498 | Shared accessor-generation helper for the namespace families. |
| `collections.py` | 1168 | The `.list`, `.struct`, `.json`, and `.map` accessor namespaces. |
| `strings.py` | 3485 | The `.str` accessor namespace. |
| `temporal.py` | 982 | The `.dt` accessor namespace plus the Polars-style offset-string parser. |

### `batcher/plan/expr_ir/selectors/` — 1 · contract

Column selectors — expressions that stand for *many* columns at plan time.

| module | lines | what it is |
|---|---|---|
| `build.py` | 285 | The public selector constructors — ``bt.all()``, ``bt.numeric()``, ``bt.matches(...)``. |
| `core.py` | 345 | The `Selector` expression leaf and its `.name` rename accessor. |
| `expand.py` | 155 | Resolving a selector-bearing expression against a schema. |

### `batcher/plan/expr_rewrite/` — 1 · contract

Shared traversal for scalar `Expr` trees and for the expressions inside a node.

| module | lines | what it is |
|---|---|---|
| `algebra.py` | 175 | Boolean-connective algebra, column substitution, and window hoisting. |
| `nodes.py` | 123 | Apply an expression rewrite to every expression a *plan node* carries. |
| `subtrees.py` | 57 | Structural identity of an expression, and whole-subtree substitution. |
| `traverse.py` | 223 | The structural ladder for scalar `Expr` trees — child access and rebuilding. |

### `batcher/plan/functions/` — 1 · contract

The expression function library, grouped by family.

| module | lines | what it is |
|---|---|---|
| `aggregate.py` | 475 | Aggregate free functions that compose existing mergeable aggregates. |
| `collection.py` | 125 | Collection-construction free functions (`struct`, `named_struct`, `sequence`). |
| `conditional.py` | 59 | Conditional / null-handling free functions (`iff`, `nanvl`). |
| `horizontal.py` | 298 | Row-wise ("horizontal") reductions across several columns. |
| `math.py` | 162 | Math free functions that compose existing scalar ops. |
| `quantiles.py` | 117 | Quantile, cardinality, and histogram aggregate shorthands. |
| `regression.py` | 229 | Linear-regression aggregate functions (DuckDB/PostgreSQL ``regr_*`` family). |
| `security.py` | 286 | Data-protection functions: `mask`, `hmac_sha256`, `aes_encrypt`, `aes_decrypt`. |
| `statistics.py` | 502 | Derived statistical aggregates built as expressions over mergeable primitives. |
| `string.py` | 117 | String-building free functions (`concat`, `concat_ws`, `format_string`). |
| `temporal.py` | 234 | Temporal free functions. |

### `batcher/plan/logical/` — 1 · contract

`LogicalPlan` — the declarative plan the public API builds.

| module | lines | what it is |
|---|---|---|
| `aggregate.py` | 168 | Grouping and ordering logical nodes: `Aggregate` and `Sort` (and their specs). |
| `base.py` | 157 | `LogicalPlan` — the base class for declarative plan nodes. |
| `join.py` | 247 | Join logical nodes: `JoinOutputCol` and `Join`. |
| `relational.py` | 354 | Row-wise and set relational logical nodes. |
| `reshape.py` | 219 | Row-reshaping logical nodes — `plan`, the neutral contract layer. |
| `transforms.py` | 213 | Plan transforms and predicates over `LogicalPlan` trees. |
| `window.py` | 253 | Window-function logical nodes: `WindowFuncSpec` and `Window`. |

### `batcher/plan/profile/` — 1 · contract

Query profiles — the planned plan joined to the measured run, for `EXPLAIN`.

| module | lines | what it is |
|---|---|---|
| `collect.py` | 269 | Profile assembly — join Kyber's estimates to Core's measurements by `op_id`. |
| `types.py` | 323 | Profile value types and rendering — `Decision`, `OpProfile`, `QueryProfile`. |

### `batcher/plan/streaming/` — 1 · contract

`plan.streaming` — neutral streaming-query contract types.

| module | lines | what it is |
|---|---|---|
| `spec.py` | 273 | Neutral streaming-query specification types — triggers, output modes, progress. |

### `batcher/plan/types/` — 1 · contract

The neutral type vocabulary and inference for the plan layer.

| module | lines | what it is |
|---|---|---|
| `infer.py` | 465 | Per-expression output-type inference — a column's Arrow type before the engine runs. |
| `lattice.py` | 88 | The lossless numeric type lattice and the FFI narrow-widening mirror. |
| `registry.py` | 42 | The dtype-name ↔ Arrow-type vocabulary — the canonical cast-name table. |
| `widths.py` | 96 | Static per-column byte widths derived from a column's Arrow type. |

### `batcher/metadata/` — 1 · contract

`metadata` — the MetadataHub: durable, learned, Ray-object-store-free.

| module | lines | what it is |
|---|---|---|
| `hub.py` | 350 | `MetadataHub` — the façade over a `MetadataBackend`. |
| `io_stats.py` | 123 | Observed per-source I/O throughput — measured on read, captured for prediction. |
| `source_stats_store.py` | 128 | Persisted source statistics — remember what Batcher wrote, for the next read. |
| `store.py` | 33 | The pluggable persistence abstraction behind the MetadataHub. |

### `batcher/metadata/backends/` — 1 · contract

MetadataHub persistence backends.

| module | lines | what it is |
|---|---|---|
| `in_process.py` | 32 | In-process dict backend — for tests and single-process runs. |
| `layered.py` | 74 | Layered backend — a fast in-process cache over a durable shared store. |
| `object_storage.py` | 85 | Object-storage backend — durable, cluster-shared learned statistics. |
| `redis.py` | 61 | Redis backend — low-latency, cluster-shared learned statistics. |
| `sqlite.py` | 64 | SQLite backend — the local durable default. |

### `batcher/config/` — 0 · utility

Configuration: one frozen, typed `Config` object.

| module | lines | what it is |
|---|---|---|
| `config.py` | 1945 | The single frozen `Config` and its typed sections. |
| `profiles.py` | 271 | Named fault-tolerance profiles for the distributed engine. |
| `validation.py` | 462 | Config range/consistency validation. |

### `batcher/_internal/` — 0 · utility

`_internal` — layer 0: what every layer above may depend on, and nothing more.

| module | lines | what it is |
|---|---|---|
| `errors.py` | 162 | The Batcher exception hierarchy. |
| `events.py` | 199 | The engine's one observability event bus — every subsystem publishes here. |
| `hardware.py` | 490 | Effective hardware detection — the CPU parallelism the process may actually use. |
| `logging.py` | 245 | Centralized logging for the whole engine — one configured `batcher.*` hierarchy. |
| `native.py` | 82 | The single accessor for the compiled Rust data plane (``batcher._native``). |
| `optional.py` | 73 | The one optional-dependency import guard. |
| `paths.py` | 26 | Filesystem locations of the installed package. |
| `prefetch.py` | 53 | Overlap a producer generator with its consumer on a background thread. |
| `registry.py` | 63 | A single generic registry pattern, used for every extension point. |

## Rust data plane — `crates/`

Crates in dependency order (dependents first). The `depends on` line is read from each `Cargo.toml`, so it is the real DAG, not a remembered one. Rust line counts exclude the trailing `#[cfg(test)]` module, matching `lint-structure`.

### `bc-py`

`bc-py` — the PyO3 boundary that assembles the Rust engine into the `batcher._native` extension module.

**depends on:** `bc-arrow`, `bc-interp`, `bc-io`, `bc-ir`, `bc-resource`, `bc-sketches`, `bc-transport`

| file | lines | what it is |
|---|---|---|
| `bloom.rs` | 197 | Bloom-filter FFI for the distributed runtime join reduction. |
| `errors.rs` | 47 | Classified shuffle-fetch exceptions at the PyO3 boundary. |
| `flight.rs` | 406 | Flight FFI: the Arrow Flight shuffle transport surface exposed to Python. |
| `lib.rs` | 620 | `bc-py` — the PyO3 boundary that assembles the Rust engine into the `batcher._native` extension module. |
| `normalize.rs` | 389 | Boundary type normalization: the input/output type adaptations the FFI applies so the engine's kernels stay on a small, well-tested set of column types. |
| `process.rs` | 85 | Process-wide singletons the FFI layer shares across calls. |
| `shuffle.rs` | 548 | Shuffle FFI: partitioners and the concurrent reducer gather. |
| `sketches.rs` | 553 | Sketch / statistics FFI: HyperLogLog distinct counts, KLL/TDigest quantiles, Misra-Gries heavy hitters, and reservoir sampling over Arrow batches. |
| `tracing_init.rs` | 104 | Rust data-plane `tracing` → Python `logging` bridge. |

### `bc-interp`

`bc-interp` — the Tier-0 interpreter.

**depends on:** `bc-arrow`, `bc-codegen`, `bc-expr`, `bc-ir`, `bc-resource`, `bc-runtime`

| file | lines | what it is |
|---|---|---|
| `agg_par.rs` | 215 | The high-cardinality parallel aggregate: partition first, aggregate once. |
| `dist.rs` | 441 | Distributed-execution primitives. |
| `error.rs` | 110 | The crate's error type: plan-interpretation failures, plus the expression and runtime errors it wraps from the crates below it. |
| `join_par.rs` | 320 | Parallel join strategies shared by the multi-core executor (`par`). |
| `lib.rs` | 655 | `bc-interp` — the Tier-0 interpreter. |
| `metrics.rs` | 251 | Per-operator execution metrics — the measure half of the adaptive loop. |
| `ops/external_sort.rs` | 318 | Out-of-core sort: spill sorted runs and merge them with bounded fan-in. |
| `ops/joins.rs` | 516 | Join per-batch primitives: equi (`join_batches`) and ASOF (`asof_join_batches`). |
| `ops/materialize.rs` | 241 | Concatenating morsels back into one batch — the first step of every pipeline breaker (sort / join / asof / window). |
| `ops/mixed_spill.rs` | 248 | Bounded out-of-core aggregation for a *mix* of value-list and constant-state aggregates in one `GROUP BY`. |
| `ops/mod.rs` | 1020 | Per-batch / per-side operator primitives shared by the sequential reference executor (`crate::execute`) and the parallel executor (`crate::par`). |
| `ops/morsel.rs` | 486 | Morselization: splitting input batches into row- **and** byte-bounded morsels for the parallel scheduler. |
| `ops/project_field.rs` | 83 | Output-field construction for [`super::project_batch_jit`]. |
| `ops/quantile_spill/histogram.rs` | 214 | Bounded out-of-core `histogram(value)` — the `Map<value, count>` member of the value-list aggregate family (`super`), split out so the parent module stays within the file-size budget. |
| `ops/quantile_spill/mod.rs` | 735 | Bounded out-of-core exact value-list aggregates for a single grouped aggregate. |
| `ops/radix_sort.rs` | 176 | LSD radix sort for fixed-width integer / temporal / float sort keys. |
| `ops/repartition.rs` | 242 | Hash-partition a relation held as morsels, gathering each row exactly **once**. |
| `ops/reshape.rs` | 452 | Row-reshaping per-batch primitives: `unnest`/`explode`, `unpivot`/`melt`, and content-hash `sample`. |
| `ops/sample_sort.rs` | 321 | Single-node parallel full sort by **sample-sort**. |
| `ops/str_sort.rs` | 71 | Stable sort permutation for a `Utf8` / `LargeUtf8` sort key. |
| `par.rs` | 2686 | The multi-core executor. |
| `stream/breaker.rs` | 302 | The breakers: operators that must see all of their input before they can emit any output. |
| `stream/meter.rs` | 212 | Per-operator metrics for the streaming executor. |
| `stream/mod.rs` | 800 | Tier-0 **streaming** executor: pull morsels through the linear runs, materialize only at breakers. |
| `stream/parallel.rs` | 711 | Streaming, across cores: one pipeline instance per worker over a shard of the driving scan. |
| `stream/pipeline.rs` | 152 | The lazy pipeline adapters: scan, the per-morsel transforms, and the early-exiting limit. |
| `window_spill.rs` | 72 | Bounded-memory window execution via grace partitioning. |

### `bc-runtime`

`bc-runtime` — the engine's runtime library.

**depends on:** `bc-arrow`, `bc-ir`, `bc-sketches`

| file | lines | what it is |
|---|---|---|
| `agg/accum.rs` | 527 | Per-type accumulator helpers for `sum`/`min`/`max` and the masked-array and concat utilities they share. |
| `agg/argextreme.rs` | 97 | ARG_MIN / ARG_MAX — the value at the row with the extreme (min/max) ordering key. |
| `agg/distinct.rs` | 367 | COUNT(DISTINCT) — exact, mergeable via a per-group value list — plus the `bucket_values_into_list` helper shared with the median path and the single-pass… |
| `agg/fused.rs` | 418 | Fused multi-aggregate accumulation — read `group_ids` once for all simple scalar aggregates instead of once per aggregate. |
| `agg/group/assign.rs` | 1041 | Assign each row of a batch a dense group id — the per-morsel hot path of every hash aggregate, `DISTINCT`, and partitioned window. |
| `agg/group/combine.rs` | 430 | Parallel hash-radix `combine` regroup for a high-cardinality aggregate. |
| `agg/group/mod.rs` | 24 | Group-key assignment and the parallel `combine` regroup. |
| `agg/hll.rs` | 99 | APPROX_COUNT_DISTINCT — bounded-memory distinct count via per-group HyperLogLog. |
| `agg/median.rs` | 371 | MEDIAN / continuous-quantile — exact, mergeable via a per-group value list (no dedup, unlike COUNT(DISTINCT)). |
| `agg/mod.rs` | 670 | Hash aggregation — built mergeable so the SAME code runs single-node and distributed. |
| `agg/qsketch.rs` | 87 | APPROX_QUANTILE / APPROX_MEDIAN — bounded-memory quantiles via per-group DDSketch. |
| `agg/spill.rs` | 651 | Spilling (grace) hash aggregation — bounded-memory `combine` + `finalize`. |
| `agg/stats.rs` | 358 | Two-input covariance/correlation and single-input skewness/kurtosis. |
| `agg/var.rs` | 200 | Variance / standard-deviation / mean finalizers and their shared (sum, sum_of_squares, count) partial-state producer. |
| `error.rs` | 38 | The crate's error type: how the stateful runtime structures report failure. |
| `gather.rs` | 95 | Column gather (`take`) with a fast path for variable-length string columns. |
| `join/asof.rs` | 137 | ASOF (nearest-match) join: each left row matched to the right row whose `on` key is nearest in a direction within its `by` group. |
| `join/build.rs` | 155 | Parallel hash-table build — shard the heads by hash so every core builds at once. |
| `join/mod.rs` | 1261 | Hash join — produces match index-pairs, built to distribute. |
| `join/radix.rs` | 123 | Parallel radix partitioning — the scatter pass shared by both radix joins. |
| `join/scratch_bench.rs` | 2 | TEMPORARY scratch measurement — not part of the crate's contract. |
| `join/sort_merge.rs` | 180 | Sort-merge equi-join: the no-hash-table join for two large (or already-sorted) inputs. |
| `join/stream.rs` | 205 | Streaming broadcast probe — build the hash table once, probe one morsel at a time. |
| `keys.rs` | 246 | The one canonical form for grouping/partitioning keys. |
| `lib.rs` | 34 | `bc-runtime` — the engine's runtime library. |
| `shuffle.rs` | 934 | Hash repartitioning — the shuffle primitive. |
| `window.rs` | 1070 | Window functions — partition, order, and append one column per function. |
| `window_fill.rs` | 53 | `forward_fill` / `backward_fill` — carry the nearest non-null value along an ordered partition. |
| `window_frame.rs` | 704 | Explicit `ROWS` window frames — sliding-window aggregates. |
| `window_parallel.rs` | 277 | Bucket-parallel window execution: hash-partition rows by the PARTITION BY keys so every window partition lands wholly inside one bucket, run the serial window kernel ([`crate::window::window_serial`]) on each bucket across rayon cores, and scatter each function's output column back to original row order. |
| `window_partition_agg.rs` | 303 | Whole-partition window aggregates (`SUM`/`AVG`/`MIN`/`MAX`/`COUNT` with no ORDER BY and no frame): one value per partition, broadcast to every row of that… |

### `bc-codegen`

`bc-codegen` — a Cranelift JIT backend for `bc-expr` scalar expressions.

**depends on:** `bc-arrow`, `bc-expr`

| file | lines | what it is |
|---|---|---|
| `analyze.rs` | 330 | Validate a [`bc_expr::Expr`] against the JIT's supported subset and infer the scalar type each sub-expression evaluates to, recording referenced columns. |
| `cache.rs` | 97 | A process-wide memo for compiled expressions. |
| `compile.rs` | 485 | Build and JIT-compile a Cranelift function that evaluates an `Expr` element-wise over the row index, returning the finalized function pointer. |
| `emit.rs` | 495 | Per-element IR emitter: recurses over a validated `Expr` building Cranelift values at the current loop index, producing one output element per row. |
| `kleene.rs` | 96 | Kleene / null-propagation support analysis for the JIT. |
| `lib.rs` | 730 | `bc-codegen` — a Cranelift JIT backend for `bc-expr` scalar expressions. |
| `simd.rs` | 346 | Vector (SIMD) emitter for the JIT's vectorizable `Expr` subset. |

### `bc-ir`

`bc-ir` — the query intermediate representation.

**depends on:** `bc-arrow`, `bc-expr`

| file | lines | what it is |
|---|---|---|
| `engine_config.rs` | 179 | Execution tunables shipped from the Python control plane alongside the plan. |
| `error.rs` | 13 | The crate's error type: how a malformed plan IR is rejected at the wire boundary. |
| `lib.rs` | 576 | `bc-ir` — the query intermediate representation. |

### `bc-expr`

`bc-expr` — scalar expression IR and its evaluation.

**depends on:** `bc-arrow`, `bc-secrets`

| file | lines | what it is |
|---|---|---|
| `analyze.rs` | 95 | Cheap static predicates over `Expr` trees, consulted *before* execution. |
| `error.rs` | 81 | The crate's error type: every way scalar expression evaluation can fail. |
| `eval/binary.rs` | 638 | Binary-operator evaluation for `Expr::Binary` plus the shared numeric/boolean coercion helpers (split out of `lib.rs`). |
| `eval/cast.rs` | 464 | `cast` evaluation with DuckDB float→int rounding semantics. |
| `eval/date.rs` | 717 | Date/time evaluation for `Expr::Date`/`DateTrunc`, dtype parsing, and the month-shift used by `BinaryOp::AddMonths` (split out of `lib.rs`). |
| `eval/dispatch.rs` | 370 | The `Expr::eval` dispatch — split out of `lib.rs` so the wire-contract enum definitions stay there and the (large) per-variant dispatch lives here. |
| `eval/generate.rs` | 83 | Series generation for `Expr::Sequence` (`sequence`/`range`). |
| `eval/hash.rs` | 232 | `Expr::Hash` — a deterministic, typed 64-bit row hash. |
| `eval/in_list.rs` | 182 | `x IN (lit, lit, …)` — hash-set membership. |
| `eval/list.rs` | 804 | List/struct evaluation for `Expr::List`/`ListGet`/`ListContains`/`StructField` (split out of `lib.rs`). |
| `eval/list_ops/coerce.rs` | 108 | Input coercion and the numeric inner loop shared by the vector-distance kernels. |
| `eval/list_ops/list_hof.rs` | 81 | Higher-order list ops for `Expr::ListTransform` / `Expr::ListFilter` (the `.list.transform` / `.list.filter` accessors). |
| `eval/list_ops/list_reduce.rs` | 161 | Per-row, list-returning numeric transforms for `eval/list.rs` (`normalize`, `softmax`, `arg_sort`, `cum_sum`, `diff`). |
| `eval/list_ops/list_reshape.rs` | 64 | Reshaping `List`-column operations that change nesting depth — currently `flatten` (`List<List<T>>` → `List<T>`). |
| `eval/list_ops/list_set.rs` | 123 | Set operations between two `List` columns for `Expr::ListSet` (`array_intersect`/`array_except`/`array_union`). |
| `eval/list_ops/list_zip.rs` | 75 | Element-wise arithmetic between two numeric `List` columns for `Expr::ListZip` (`list_add`/`list_subtract`/`list_multiply`) — the embedding-math primitive. |
| `eval/list_ops/mod.rs` | 21 | Extended `List`-column operations beyond the per-row reductions in `eval/list.rs`: set operations between two lists (`intersect`/`except`/`union`) and the higher-order `transform`/`filter` over an element sub-expression, and the SimHash LSH signature of an embedding, and the input coercion plus numeric inner loop the vector-distance kernels share. |
| `eval/list_ops/simhash.rs` | 141 | `simhash`: a random-hyperplane LSH signature of an embedding → `List<Int64>` of bits. |
| `eval/map.rs` | 83 | Map-column evaluation for `Expr::Map` (`map_keys`/`map_values`/`element_at`). |
| `eval/math.rs` | 432 | Numeric evaluation for `Expr::Math`/`Math2`/`Coalesce`/`Greatest`/`Least` (split out of `lib.rs`). |
| `eval/media/audio.rs` | 395 | Audio-decode evaluation for `Expr::Audio` (the `.audio` namespace). |
| `eval/media/image.rs` | 649 | Image-decode evaluation for `Expr::Image` (the `.image` namespace). |
| `eval/media/mel.rs` | 237 | Mel power-spectrogram kernel for `AudioFunc::MelSpectrogram`. |
| `eval/media/mod.rs` | 40 | Library-backed multimodal decoders (image / audio / video) for the `.image`/`.audio`/`.video` expression namespaces. |
| `eval/media/video.rs` | 159 | Video-decode evaluation for `Expr::Video` (the `.video` namespace). |
| `eval/mod.rs` | 22 | Evaluation bodies for the scalar `Expr` variants. |
| `eval/security/crypto.rs` | 101 | Keyed cryptographic primitives: HMAC-SHA-256 pseudonymization and AES-256-GCM-SIV column encryption. |
| `eval/security/keyref.rs` | 50 | Resolving a crypto key *reference* to the key material, at evaluation time. |
| `eval/security/mask.rs` | 35 | Character masking — the redaction primitive behind partial-disclosure policies ("show only the last four digits"). |
| `eval/security/mod.rs` | 115 | Data-protection string functions: `hmac_sha256`, `aes_encrypt`, `aes_decrypt`, `mask`. |
| `eval/str/chunk.rs` | 182 | `StrFunc::Chunk` — overlapping text windows (the RAG document splitter). |
| `eval/str/html.rs` | 172 | `strip_html`: recover the readable text of an HTML document. |
| `eval/str/jaro.rs` | 80 | Jaro and Jaro-Winkler string similarity (the `.str.jaro`/`.str.jaro_winkler` funcs). |
| `eval/str/json.rs` | 391 | JSON path extraction for the `.json` accessor (`json_extract_{string,int,float,bool}`). |
| `eval/str/like.rs` | 165 | Fast SQL `LIKE` / substring matching. |
| `eval/str/minhash.rs` | 146 | `StrFunc::MinHash` — a MinHash signature of a document → `List<Int64>`. |
| `eval/str/mod.rs` | 1203 | String-function evaluation for `Expr::Str` (split out of `lib.rs`). |
| `eval/str/regex_cache.rs` | 68 | A process-wide memo for compiled regexes. |
| `eval/timezone.rs` | 61 | Timezone conversion for `Expr::ConvertTimezone` (`convert_timezone`). |
| `lib.rs` | 1008 | `bc-expr` — scalar expression IR and its evaluation. |

### `bc-arrow`

`bc-arrow` — Arrow building blocks shared across the engine.

**depends on:** _(nothing — leaf crate)_

| file | lines | what it is |
|---|---|---|
| `float_ident.rs` | 154 | The engine's one definition of **float identity**. |
| `hardware.rs` | 190 | Host CPU capability detection for adaptive execution. |
| `lib.rs` | 215 | `bc-arrow` — Arrow building blocks shared across the engine. |

### `bc-sketches`

`bc-sketches` — mergeable probabilistic sketches for the optimizer.

**depends on:** _(nothing — leaf crate)_

| file | lines | what it is |
|---|---|---|
| `bloom.rs` | 155 | Bloom filter — approximate set membership for runtime join filters. |
| `countmin.rs` | 149 | Count-Min — frequency (heavy-hitter) estimation. |
| `ddsketch.rs` | 368 | DDSketch — relative-error quantile sketch (Masson, Rim, Lee). |
| `frequent.rs` | 199 | Misra-Gries — frequent-items (heavy-hitter *key*) enumeration. |
| `hll.rs` | 295 | HyperLogLog++ — distinct-count (cardinality) estimation. |
| `kll.rs` | 453 | KLL — streaming quantile / rank sketch (Karnin–Lang–Liberty). |
| `lib.rs` | 74 | `bc-sketches` — mergeable probabilistic sketches for the optimizer. |
| `reservoir.rs` | 306 | Reservoir sampling — a fixed-size uniform random sample of a stream. |
| `stats.rs` | 300 | Per-column statistics derived from a single scan. |
| `tdigest.rs` | 422 | T-Digest — tail-accurate quantile sketch (Dunning). |

### `bc-transport`

Arrow Flight inter-node transport for Batcher's distributed shuffle.

**depends on:** _(nothing — leaf crate)_

| file | lines | what it is |
|---|---|---|
| `exchange.rs` | 624 | The node-level [`ShuffleExchange`]: the ergonomic API the distributed layer calls to publish and fetch shuffle partitions between nodes with credit-bounded… |
| `handler.rs` | 368 | The [`FlightService`] implementation backing a `FlightServer`, plus the credit-grant encode/decode helpers it shares with the exchange client. |
| `lib.rs` | 105 | Arrow Flight inter-node transport for Batcher's distributed shuffle. |
| `shared.rs` | 240 | Same-node, cross-process partition transfer via memory-mapped Arrow IPC. |
| `store.rs` | 143 | Internal partition store: the in-memory registry mapping a ticket string to the batches served under it, plus the per-exchange in-flight gauge used to prove… |
| `ticket.rs` | 91 | The structured shuffle coordinate ([`ShuffleTicket`]) the distributed layer uses to build and parse the opaque ticket string carried on the wire. |
| `tls.rs` | 158 | TLS configuration for the inter-node Flight shuffle. |
| `tls_test_certs.rs` | 193 | Static PEM test material for the TLS transport tests, minted with openssl. |

### `bc-resource`

Process-wide memory accounting for reserve-before-allocate.

**depends on:** _(nothing — leaf crate)_

| file | lines | what it is |
|---|---|---|
| `lib.rs` | 354 | Process-wide memory accounting for reserve-before-allocate. |

### `bc-io`

Native Rust format readers (Parquet over object storage; Avro OCF to Arrow).

**depends on:** _(nothing — leaf crate)_

| file | lines | what it is |
|---|---|---|
| `avro.rs` | 31 | Native Avro (object-container-file) decode to Arrow, via `arrow-avro`. |
| `bloom.rs` | 166 | Bloom-filter pruning: skip a row group whose bloom proves an equality cannot match. |
| `footer_stats.rs` | 561 | Aggregate Parquet footer statistics across many files, natively. |
| `lib.rs` | 426 | Native Rust format readers (Parquet over object storage; Avro OCF to Arrow). |
| `page_index.rs` | 271 | Page-level pruning: turn a pushed predicate into a `RowSelection` over one row group. |
| `predicate.rs` | 281 | Row-group pruning from a pushed predicate's zone maps (footer statistics). |
| `store.rs` | 170 | Resolve a URI to an `object_store` backend + in-store path, for every scheme the engine reads: `s3://` (and on-prem S3 like MinIO/Ceph via an endpoint… |

### `bc-udf`

The opaque-operator boundary + dynamic-batching machinery for the UDF / ML inference plane.

**depends on:** `bc-arrow`

| file | lines | what it is |
|---|---|---|
| `batch_size.rs` | 87 | [`BatchSizeController`] — a PID governor for the dynamic batch size. |
| `lib.rs` | 49 | The opaque-operator boundary + dynamic-batching machinery for the UDF / ML inference plane. |
| `operator.rs` | 110 | The [`OpaqueOperator`] trait and its concrete [`FnOperator`] seam. |
| `rebatch.rs` | 120 | [`Rebatcher`] — re-chunk a stream of batches to a target row count. |

### `bc-secrets`

Resolving a secret *reference* to secret material, on the machine that needs it.

**depends on:** _(nothing — leaf crate)_

| file | lines | what it is |
|---|---|---|
| `lib.rs` | 242 | Resolving a secret *reference* to secret material, on the machine that needs it. |
