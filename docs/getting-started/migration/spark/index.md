<!-- Generated by tools/gen_migration_docs.py from the migration registry in python/batcher/_internal/migration/data/pyspark/. Do not edit by hand: change the registry, then run `just migration-docs`. -->

# PySpark to Batcher

This section maps every public name in PySpark 4.2.0 onto its Batcher spelling, one page per family of names, and maps the shared spellings back again for code leaving Batcher. It covers 955 names.

The pages are generated from the migration registry in `python/batcher/_internal/migration/data/pyspark/`, which classifies each name in the PySpark surface snapshot exactly once. The `AttributeError` you get when you type a PySpark name on a Batcher object reads the same rows, so the message and these tables agree.

## Rewrite code with the codemod

The codemod rewrites PySpark calls onto Batcher spellings:

```bash
python -m batcher.migrate --from pyspark --to batcher <paths>
```

Replace `<paths>` with the files or directories to rewrite. This direction is implemented. Without `--write` the command prints a diff and changes nothing, and `--write` applies it.

## How to read the tables

Every row names the PySpark spelling, the Batcher spelling, a status, and notes. The status is the part to read first, because a matching name is not a matching result. The following table defines each status:

| Status | Meaning |
|---|---|
| canonical | Both engines compute the same thing. Use the Batcher spelling. |
| alias | Batcher once accepted this name as a second spelling. Batcher keeps one spelling per capability, so use the Batcher spelling instead. |
| param | The capability exists, but Batcher lacks an option the other engine offers. The notes say which. |
| mismatch | Both engines have the capability and give different answers. The notes say what differs. Never rename one of these without reading them. |
| gap | Batcher has no equivalent yet. The notes say what is missing. |
| out of scope | Batcher deliberately doesn't provide it. The notes say why. |

A Batcher spelling is a dotted path from the object you call it on: `Dataset` is a dataset, `Expr` an expression, `GroupBy` the result of `group_by`, and `bt` the `batcher` package. Spellings joined by `+` are used together.

## Waves

A row that isn't at parity carries a *wave*: the planned unit of work that closes it. Waves are an order of work, not release dates. Silent mismatches come first, because they return different answers without an error. Cheap surface wiring comes next, then new kernels, then the large families. A row's status changes in the same commit that closes it, so these pages track progress when they're regenerated.

The following table lists the waves that hold PySpark rows, in delivery order:

| Wave | Scope | Rows |
|---|---|---|
| WF | Foundations: output-name inference and aggregate broadcasting in `select`, `when` without `otherwise`, `over` on every expression, one explicit row-order policy, and the UDF verbs consolidated onto `Dataset` | 5 |
| W0 | Silent mismatches: parameters that restore the other engine's semantics | 97 |
| W1 | Python constructors over SQL functions Batcher already has | 29 |
| W2 | Missing parameters across all four engines | 69 |
| W3 | Scalar functions | 28 |
| W4 | Maps, higher-order functions, and list reduction | 16 |
| W5 | Window functions, including rolling and dynamic group-by | 4 |
| W6 | Temporal types: `TIME`, intervals, and a session time zone | 38 |
| W7 | Semi-structured data: JSON, CSV, and XML functions | 31 |
| W8 | Relational `DataFrame` gaps | 29 |
| W9 | Catalogs and tables | 71 |
| W10 | Streaming | 28 |
| W11 | UDFs | 61 |
| W13 | Readers and writers | 9 |
| W14 | Sketches, collations, and the geography type | 73 |

## Pages

The following table lists the pages in this section with the number of names each one covers, by status:

| Page | Names | Canonical or alias | Param | Mismatch | Gap | Out of scope |
|---|---|---|---|---|---|---|
| {doc}`dataframe` | 152 | 51 | 47 | 26 | 22 | 6 |
| {doc}`expressions` | 36 | 26 | 2 | 4 | 4 | 0 |
| {doc}`functions-aggregates` | 135 | 45 | 13 | 9 | 68 | 0 |
| {doc}`functions-collections` | 90 | 19 | 12 | 9 | 50 | 0 |
| {doc}`functions-math-and-misc` | 133 | 65 | 13 | 7 | 40 | 8 |
| {doc}`functions-strings` | 93 | 38 | 6 | 24 | 25 | 0 |
| {doc}`functions-temporal` | 88 | 30 | 11 | 15 | 32 | 0 |
| {doc}`io` | 70 | 23 | 19 | 16 | 12 | 0 |
| {doc}`session-and-sql` | 101 | 36 | 10 | 2 | 46 | 7 |
| {doc}`types-and-data-sources` | 57 | 13 | 34 | 2 | 8 | 0 |

{doc}`leaving-batcher` maps the other way, from Batcher spellings back to PySpark.

## See also

- {doc}`../differences`: the concepts that change in any port, such as laziness and write-mode defaults.
- {doc}`../index`: the migration guide, with the other source engines.

```{toctree}
:hidden:

dataframe
expressions
functions-aggregates
functions-collections
functions-math-and-misc
functions-strings
functions-temporal
io
session-and-sql
types-and-data-sources
leaving-batcher
```
