<!-- Generated by tools/gen_migration_docs.py from the migration registry in python/batcher/_internal/migration/data/daft/. Do not edit by hand: change the registry, then run `just migration-docs`. -->

# Daft to Batcher

This section maps every public name in Daft 0.7.25 onto its Batcher spelling, one page per family of names, and maps the shared spellings back again for code leaving Batcher. It covers 1025 names.

The pages are generated from the migration registry in `python/batcher/_internal/migration/data/daft/`, which classifies each name in the Daft surface snapshot exactly once. The `AttributeError` you get when you type a Daft name on a Batcher object reads the same rows, so the message and these tables agree.

## Rewrite code with the codemod

The codemod rewrites Daft calls onto Batcher spellings:

```bash
python -m batcher.migrate --from daft --to batcher <paths>
```

Replace `<paths>` with the files or directories to rewrite. This direction is implemented. Without `--write` the command prints a diff and changes nothing, and `--write` applies it.

## How to read the tables

Every row names the Daft spelling, the Batcher spelling, a status, and notes. The status is the part to read first, because a matching name is not a matching result. The following table defines each status:

| Status | Meaning |
|---|---|
| canonical | Both engines compute the same thing. Use the Batcher spelling. |
| alias | Batcher once accepted this name as a second spelling. Batcher keeps one spelling per capability, so use the Batcher spelling instead. |
| param | The capability exists, but Batcher lacks an option the other engine offers. The notes say which. |
| mismatch | Both engines have the capability and give different answers. The notes say what differs. Never rename one of these without reading them. |
| gap | Batcher has no equivalent yet. The notes say what is missing. |
| out of scope | Batcher deliberately doesn't provide it. The notes say why. |

A Batcher spelling is a dotted path from the object you call it on: `Dataset` is a dataset, `Expr` an expression, `GroupBy` the result of `group_by`, and `bt` the `batcher` package. Spellings joined by `+` are used together.

## Waves

A row that isn't at parity carries a *wave*: the planned unit of work that closes it. Waves are an order of work, not release dates. Silent mismatches come first, because they return different answers without an error. Cheap surface wiring comes next, then new kernels, then the large families. A row's status changes in the same commit that closes it, so these pages track progress when they're regenerated.

The following table lists the waves that hold Daft rows, in delivery order:

| Wave | Scope | Rows |
|---|---|---|
| WF | Foundations: output-name inference and aggregate broadcasting in `select`, `when` without `otherwise`, `over` on every expression, one explicit row-order policy, and the UDF verbs consolidated onto `Dataset` | 6 |
| W0 | Silent mismatches: parameters that restore the other engine's semantics | 49 |
| W1 | Python constructors over SQL functions Batcher already has | 5 |
| W2 | Missing parameters across all four engines | 104 |
| W3 | Scalar functions | 31 |
| W4 | Maps, higher-order functions, and list reduction | 12 |
| W5 | Window functions, including rolling and dynamic group-by | 10 |
| W6 | Temporal types: `TIME`, intervals, and a session time zone | 38 |
| W7 | Semi-structured data: JSON, CSV, and XML functions | 10 |
| W8 | Relational `DataFrame` gaps | 11 |
| W9 | Catalogs and tables | 111 |
| W11 | UDFs | 81 |
| W12 | AI and multimodal | 102 |
| W13 | Readers and writers | 33 |

## Pages

The following table lists the pages in this section with the number of names each one covers, by status:

| Page | Names | Canonical or alias | Param | Mismatch | Gap | Out of scope |
|---|---|---|---|---|---|---|
| {doc}`dataframe` | 115 | 54 | 20 | 28 | 11 | 2 |
| {doc}`module` | 130 | 21 | 26 | 12 | 59 | 12 |
| {doc}`functions-numeric` | 160 | 128 | 23 | 4 | 5 | 0 |
| {doc}`functions-strings` | 121 | 52 | 46 | 18 | 5 | 0 |
| {doc}`functions-temporal` | 100 | 56 | 13 | 9 | 22 | 0 |
| {doc}`functions-nested` | 157 | 67 | 29 | 14 | 38 | 9 |
| {doc}`udfs-ai-multimodal` | 63 | 0 | 22 | 18 | 23 | 0 |
| {doc}`session-and-catalog` | 81 | 4 | 15 | 1 | 60 | 1 |
| {doc}`types` | 98 | 16 | 9 | 0 | 72 | 1 |

{doc}`leaving-batcher` maps the other way, from Batcher spellings back to Daft.

## See also

- {doc}`../differences`: the concepts that change in any port, such as laziness and write-mode defaults.
- {doc}`../index`: the migration guide, with the other source engines.

```{toctree}
:hidden:

dataframe
module
functions-numeric
functions-strings
functions-temporal
functions-nested
udfs-ai-multimodal
session-and-catalog
types
leaving-batcher
```
