<!-- Generated by tools/gen_migration_docs.py from the migration registry in python/batcher/_internal/migration/data/polars/. Do not edit by hand: change the registry, then run `just migration-docs`. -->

# Polars to Batcher

This section maps every public name in Polars 1.40.0 onto its Batcher spelling, one page per family of names, and maps the shared spellings back again for code leaving Batcher. It covers 985 names.

The pages are generated from the migration registry in `python/batcher/_internal/migration/data/polars/`, which classifies each name in the Polars surface snapshot exactly once. The `AttributeError` you get when you type a Polars name on a Batcher object reads the same rows, so the message and these tables agree.

## Rewrite code with the codemod

The codemod rewrites Polars calls onto Batcher spellings:

```bash
python -m batcher.migrate --from polars --to batcher <paths>
```

Replace `<paths>` with the files or directories to rewrite. This direction is implemented. Without `--write` the command prints a diff and changes nothing, and `--write` applies it.

## How to read the tables

Every row names the Polars spelling, the Batcher spelling, a status, and notes. The status is the part to read first, because a matching name is not a matching result. The following table defines each status:

| Status | Meaning |
|---|---|
| canonical | Both engines compute the same thing. Use the Batcher spelling. |
| alias | Batcher once accepted this name as a second spelling. Batcher keeps one spelling per capability, so use the Batcher spelling instead. |
| param | The capability exists, but Batcher lacks an option the other engine offers. The notes say which. |
| mismatch | Both engines have the capability and give different answers. The notes say what differs. Never rename one of these without reading them. |
| gap | Batcher has no equivalent yet. The notes say what is missing. |
| out of scope | Batcher deliberately doesn't provide it. The notes say why. |

A Batcher spelling is a dotted path from the object you call it on: `Dataset` is a dataset, `Expr` an expression, `GroupBy` the result of `group_by`, and `bt` the `batcher` package. Spellings joined by `+` are used together.

## Waves

A row that isn't at parity carries a *wave*: the planned unit of work that closes it. Waves are an order of work, not release dates. Silent mismatches come first, because they return different answers without an error. Cheap surface wiring comes next, then new kernels, then the large families. A row's status changes in the same commit that closes it, so these pages track progress when they're regenerated.

The following table lists the waves that hold Polars rows, in delivery order:

| Wave | Scope | Rows |
|---|---|---|
| WF | Foundations: output-name inference and aggregate broadcasting in `select`, `when` without `otherwise`, `over` on every expression, one explicit row-order policy, and the UDF verbs consolidated onto `Dataset` | 18 |
| W0 | Silent mismatches: parameters that restore the other engine's semantics | 146 |
| W2 | Missing parameters across all four engines | 143 |
| W3 | Scalar functions | 41 |
| W5 | Window functions, including rolling and dynamic group-by | 36 |
| W6 | Temporal types: `TIME`, intervals, and a session time zone | 33 |
| W7 | Semi-structured data: JSON, CSV, and XML functions | 2 |
| W8 | Relational `DataFrame` gaps | 143 |
| W9 | Catalogs and tables | 1 |
| W11 | UDFs | 74 |
| W13 | Readers and writers | 24 |

## Pages

The following table lists the pages in this section with the number of names each one covers, by status:

| Page | Names | Canonical or alias | Param | Mismatch | Gap | Out of scope |
|---|---|---|---|---|---|---|
| {doc}`dataframe` | 265 | 77 | 73 | 50 | 51 | 14 |
| {doc}`expressions` | 218 | 68 | 29 | 41 | 77 | 3 |
| {doc}`expression-namespaces` | 219 | 80 | 33 | 34 | 72 | 0 |
| {doc}`functions` | 163 | 36 | 28 | 21 | 71 | 7 |
| {doc}`io` | 38 | 11 | 18 | 1 | 7 | 1 |
| {doc}`types` | 37 | 8 | 6 | 9 | 9 | 5 |
| {doc}`session-and-sql` | 45 | 12 | 2 | 0 | 28 | 3 |

{doc}`leaving-batcher` maps the other way, from Batcher spellings back to Polars.

## See also

- {doc}`../differences`: the concepts that change in any port, such as laziness and write-mode defaults.
- {doc}`../index`: the migration guide, with the other source engines.

```{toctree}
:hidden:

dataframe
expressions
expression-namespaces
functions
io
types
session-and-sql
leaving-batcher
```
