# Getting started

Install Batcher, run a first pipeline, and learn the core model: a `Dataset` is a
lazy, immutable handle to a query plan, and expressions describe column work that
runs in the Rust engine.

```{toctree}
:maxdepth: 1

installation
quickstart
concepts
```
