# Machine learning

Apply Python functions and models to Arrow batches with the `ml` accessor. Work
happens on whole batches, never per row, and scheduling can place work on GPUs and
across actors.

```{toctree}
:maxdepth: 1

streaming
inference
pytorch
gpu
```
